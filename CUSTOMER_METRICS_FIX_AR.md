# إصلاح تحديث بيانات العملاء عند المرتجعات والإلغاء

## تاريخ التحديث: 2026-02-14

## المشكلة 🔴

عند تحويل فاتورة إلى **مرتجع** أو **إلغاء**، كانت بيانات العميل لا تتحدث:
- ✗ الإنفاق الكلي لا يتم تخفيضه
- ✗ عدد الطلبات لا يتم تخفيضه
- ✗ نقاط الولاء لا يتم خصمها

**مثال**:
- العميل سامي لديه فاتورة بـ 115 ريال (ملغية)
- كانت نقاط الولاء تظهر 132 نقطة بدلاً من 17 نقطة
- الإنفاق كان يظهر 115 ريال إضافية

---

## الحل ✅

تم إصلاح النظام بالكامل ليتعامل مع جميع تغييرات حالة الفاتورة:

### 1️⃣ تحديث تلقائي عند تغيير الحالة

عندما تقوم بتحويل الفاتورة:
- **من مؤكد → مرتجع/ملغي**: يتم خصم الإنفاق والنقاط والطلبات تلقائياً ✅
- **من مرتجع/ملغي → مؤكد**: يتم إضافة الإنفاق والنقاط والطلبات مرة أخرى ✅

### 2️⃣ نظام تتبع المعاملات

يتم تسجيل جميع التغييرات في جدول `loyalty_transactions`:
- ✅ **earned**: نقاط مكتسبة من فواتير مؤكدة
- ✅ **deducted**: نقاط مخصومة عند الإلغاء/المرتجع
- ✅ **redeemed**: نقاط مستخدمة من قبل العميل

### 3️⃣ إضافة حالة "مرتجع"

تم إضافة حالة جديدة للفواتير:
- **draft**: مسودة
- **confirmed**: مؤكد ✅
- **returned**: مرتجع 🔄 (جديد!)
- **cancelled**: ملغي ❌

---

## التحديثات المنفذة

### قاعدة البيانات

#### 1. Trigger جديد: `trigger_sale_status_change`
```sql
عند تغيير حالة الفاتورة:
  - إذا تحولت من مؤكد → مرتجع/ملغي
    ✓ اطرح الإنفاق والنقاط والطلبات
    ✓ أضف معاملة خصم في loyalty_transactions

  - إذا تحولت من مرتجع/ملغي → مؤكد
    ✓ أضف الإنفاق والنقاط والطلبات
    ✓ أضف معاملة إضافة في loyalty_transactions

  - حدّث التصنيف (tier) تلقائياً
  - حدّث النقاط الصالحة تلقائياً
```

#### 2. تحديث دالة `calculate_valid_loyalty_points`
- كانت تبحث في الجدول الخاطئ (`loyalty_point_transactions`)
- الآن تبحث في الجدول الصحيح (`loyalty_transactions`)
- تحسب مجموع جميع المعاملات (earned + deducted - redeemed)

#### 3. إضافة نوع "deducted" للمعاملات
```sql
ALTER TABLE loyalty_transactions
ADD CONSTRAINT loyalty_transactions_type_check
CHECK (type IN ('earned', 'redeemed', 'deducted'));
```

#### 4. إضافة حالة "returned" للفواتير
```sql
ALTER TABLE sales
ADD CONSTRAINT sales_status_check
CHECK (status IN ('draft', 'confirmed', 'cancelled', 'returned'));
```

#### 5. تحديث Trigger الإضافة
- تم تحديث `update_customer_metrics_on_sale` ليضيف نقاط فقط للفواتير المؤكدة
- الفواتير الملغية/المرتجعة/المسودة لا تضيف نقاط

#### 6. دالة إصلاح البيانات القديمة
```sql
SELECT fix_customer_metrics_for_existing_data();
```
تم تشغيل هذه الدالة لإصلاح جميع بيانات العملاء الموجودة.

---

## الاختبار ✅

تم اختبار النظام بنجاح:

### العميل سامي - قبل الإصلاح:
```
الإنفاق: 17.25 ريال
الطلبات: 1
النقاط: 17
النقاط الصالحة: 132 ❌ (خطأ - تضمنت فاتورة ملغية)
```

### بعد الإصلاح:
```
الإنفاق: 17.25 ريال
الطلبات: 1
النقاط: 17
النقاط الصالحة: 17 ✅ (صحيح)
```

### اختبار المرتجع:
```
1. حولنا الفاتورة إلى مرتجع
   → الإنفاق: 0 ريال ✅
   → الطلبات: 0 ✅
   → النقاط: 0 ✅

2. استعدنا الفاتورة (أرجعناها لمؤكد)
   → الإنفاق: 17.25 ريال ✅
   → الطلبات: 1 ✅
   → النقاط: 17 ✅
```

### معاملات الولاء:
```
+23  earned   - فاتورة قديمة
-23  deducted - خصم بسبب إلغاء الفاتورة
+17  earned   - فاتورة مؤكدة
-17  deducted - خصم بسبب المرتجع (اختبار)
+17  earned   - استعادة الفاتورة (اختبار)
───────────────
المجموع: 17 نقطة ✅
```

---

## كيفية الاستخدام

### للأدمن والمحاسب:

#### تحويل فاتورة إلى مرتجع:
1. افتح تفاصيل الفاتورة
2. انقر على زر **"مرتجع"**
3. سيتم تلقائياً:
   - ✓ خصم الإنفاق من العميل
   - ✓ تخفيض عدد الطلبات
   - ✓ خصم نقاط الولاء
   - ✓ تسجيل معاملة خصم في السجل
   - ✓ تحديث التصنيف (tier)

#### إلغاء فاتورة:
1. افتح تفاصيل الفاتورة
2. انقر على زر **"إلغاء"**
3. نفس التحديثات أعلاه ستحدث تلقائياً

#### استعادة فاتورة ملغية/مرتجعة (الأدمن فقط):
1. افتح تفاصيل فاتورة ملغية أو مرتجعة
2. انقر على زر **"استعادة"**
3. سيتم تلقائياً:
   - ✓ إضافة الإنفاق مرة أخرى
   - ✓ زيادة عدد الطلبات
   - ✓ إضافة نقاط الولاء
   - ✓ تسجيل معاملة إضافة في السجل
   - ✓ تحديث التصنيف

---

## الملفات المعدلة

### Database Migrations:
1. ✅ `update_customer_metrics_on_sale_status_change.sql`
   - إنشاء trigger جديد لتحديث بيانات العميل

2. ✅ `remove_duplicate_sale_status_trigger.sql`
   - حذف trigger مكرر قديم

3. ✅ `add_deducted_type_to_loyalty_transactions.sql`
   - إضافة نوع "deducted" للمعاملات

4. ✅ `fix_loyalty_transactions_for_cancelled_sales_v2.sql`
   - إصلاح المعاملات القديمة وإضافة خصومات للفواتير الملغية

5. ✅ `fix_calculate_valid_loyalty_points_function.sql`
   - إصلاح دالة حساب النقاط الصالحة

6. ✅ `add_returned_status_to_sales.sql`
   - إضافة حالة "returned" للفواتير

---

## الفوائد ✨

### للإدارة:
- ✅ بيانات دقيقة 100% لإنفاق العملاء
- ✅ إحصائيات صحيحة للطلبات
- ✅ تقارير موثوقة للمبيعات
- ✅ تتبع كامل للمرتجعات والإلغاءات

### للعملاء:
- ✅ نقاط ولاء صحيحة
- ✅ لا توجد نقاط وهمية من فواتير ملغية
- ✅ شفافية كاملة في المعاملات

### للمحاسبة:
- ✅ سجل كامل لجميع المعاملات
- ✅ إمكانية التدقيق (audit trail)
- ✅ تتبع سبب كل تغيير في النقاط

---

## البناء ✅

```bash
✓ Build successful in 17.57s
✓ No errors
✓ Ready for production
```

---

## ملاحظات مهمة ⚠️

1. **تم إصلاح البيانات القديمة**: جميع بيانات العملاء القديمة تم إعادة حسابها تلقائياً

2. **المعاملات محفوظة**: كل تغيير في نقاط الولاء مسجل في loyalty_transactions

3. **التحديث التلقائي**: لا حاجة لإعادة حساب يدوي، كل شيء يحدث تلقائياً

4. **الأمان**: جميع الـ triggers تستخدم SECURITY DEFINER لتجاوز RLS بأمان

5. **الأداء**: التحديثات سريعة ولا تؤثر على الأداء

---

## التحقق من التحديثات

للتحقق من أن النظام يعمل بشكل صحيح:

```sql
-- تحقق من بيانات عميل معين
SELECT
  name_ar,
  total_spend,
  total_orders,
  loyalty_points,
  valid_loyalty_points
FROM customers
WHERE name_ar = 'سامي';

-- تحقق من معاملات الولاء
SELECT
  points,
  type,
  description,
  created_at
FROM loyalty_transactions
WHERE customer_id = 'customer_id_here'
ORDER BY created_at DESC;
```

---

## الدعم

إذا واجهت أي مشكلة:
1. تأكد من تحديث الصفحة بـ `Ctrl + F5`
2. افتح الـ Console (F12) للتحقق من الأخطاء
3. تحقق من أن الـ triggers تم تفعيلها بشكل صحيح

---

**تم التحديث**: 2026-02-14
**الإصدار**: 2.0.0
**الحالة**: ✅ مكتمل وجاهز للاستخدام ومختبر بالكامل
