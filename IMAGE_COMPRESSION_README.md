# نظام ضغط الصور وتخزين الإيصالات

## ما تم إضافته

تم إلغاء Google Drive وتفعيل نظام تخزين صور الفواتير والإيصالات باستخدام **Supabase Storage** فقط مع ضغط تلقائي للصور.

---

## المميزات الجديدة

### 1. ضغط الصور التلقائي
- **الحد الأقصى للعرض**: 1024px
- **الحد الأقصى للحجم**: أقل من 200KB
- **الجودة**: يتم تقليلها تدريجياً حتى نصل لحجم مناسب
- **التنسيق**: تحويل جميع الصور إلى JPEG لتقليل الحجم

### 2. التخزين في Supabase
- **Bucket**: `receipts` (عام - public)
- **الملفات المدعومة**:
  - صور: JPEG, PNG, GIF, WebP
  - مستندات: PDF, Word, Excel
- **الحد الأقصى للملف**: 10MB

### 3. عرض الإيصالات
- **في الشركاء**: يمكن رفع إيصال مع كل مساهمة أو تسوية
- **في المشتريات**: يمكن رفع فاتورة مع كل عملية شراء
- **عرض مباشر**: انقر على زر "عرض المرفق" لمشاهدة الصورة/المستند في نافذة منبثقة
- **تحميل**: زر تحميل متاح في نافذة العرض

---

## كيفية الاستخدام

### في صفحة الشركاء:
1. اذهب إلى **الشركاء**
2. اضغط **إضافة مساهمة** أو **إضافة تسوية**
3. املأ البيانات واختر الإيصال من حقل "إرفاق إيصال"
4. عند الحفظ، سيتم ضغط الصورة تلقائياً ورفعها
5. لعرض الإيصال: اضغط على أيقونة التحميل بجانب المساهمة

### في صفحة المشتريات:
1. اذهب إلى **المشتريات**
2. اضغط **شراء جديد**
3. املأ البيانات واختر الفاتورة من حقل "إرفاق فاتورة"
4. عند الحفظ، سيتم ضغط الصورة تلقائياً ورفعها
5. لعرض الفاتورة: افتح تفاصيل المشتريات واضغط "تحميل الفاتورة/الإيصال"

---

## التفاصيل التقنية

### الملفات المعدلة:

1. **`src/lib/fileUpload.ts`**:
   - إضافة دالة `compressImage()` لضغط الصور
   - تحديث `uploadFile()` لاستخدام الضغط التلقائي
   - تغيير الـ bucket إلى public

2. **`src/components/Partners.tsx`**:
   - إضافة state لعرض المرفقات
   - إضافة دالة `handleViewAttachment()`
   - إضافة modal لعرض الصور/المستندات
   - تحديث أزرار عرض المرفقات

3. **`src/components/Purchases.tsx`**:
   - إضافة state لعرض المرفقات
   - إضافة دالة `handleViewAttachment()`
   - إضافة modal لعرض الصور/المستندات
   - تحديث زر عرض الفاتورة

4. **`src/components/Settings.tsx`**:
   - إزالة تبويب التخزين

### الملفات المحذوفة:
- `src/lib/googleDriveStorage.ts`
- `src/lib/storageProvider.ts`
- `src/components/StorageSettings.tsx`
- `GOOGLE_DRIVE_SETUP.md`
- `STORAGE_GUIDE_AR.md`

---

## كيفية عمل ضغط الصور

1. **قراءة الصورة**: يتم قراءة الملف باستخدام FileReader
2. **رسم على Canvas**: يتم رسم الصورة على Canvas بحجم أقصى 1024px
3. **الضغط التدريجي**:
   - يبدأ بجودة 0.8 (80%)
   - إذا كان الحجم أكبر من 200KB، يقلل الجودة بـ 0.1
   - يستمر حتى يصل لأقل من 200KB أو جودة 0.3 (30%)
4. **التحويل إلى JPEG**: جميع الصور تُحول إلى JPEG لتقليل الحجم
5. **الرفع**: يتم رفع الصورة المضغوطة إلى Supabase

---

## الفوائد

- **توفير المساحة**: الصور المضغوطة توفر أكثر من 70% من مساحة التخزين
- **سرعة الرفع**: ملفات أصغر = رفع أسرع
- **سرعة العرض**: تحميل الصور أسرع
- **تكلفة أقل**: استهلاك أقل للمساحة المجانية

---

## ملاحظات مهمة

- الملفات غير الصور (PDF, Word, Excel) لا يتم ضغطها
- إذا فشل الضغط، يتم رفع الصورة الأصلية
- الـ bucket عام (public) لعرض الصور مباشرة بدون روابط موقعة
- يتم طباعة حجم الصورة قبل وبعد الضغط في console للمراقبة

---

## مثال على التوفير

**قبل الضغط:**
- صورة عادية من الهاتف: 3-5 MB
- 100 صورة = 300-500 MB

**بعد الضغط:**
- صورة مضغوطة: أقل من 200 KB
- 100 صورة = أقل من 20 MB

**التوفير**: أكثر من 95% من المساحة!

---

**تم التطوير في:** 2026-02-13
