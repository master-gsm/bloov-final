# دليل دمج منصة سلة - Salla Integration Guide

## نظرة عامة
تم دمج منصة سلة بالكامل في نظام BLOOV المحاسبي، مما يتيح تتبع مبيعات المتجر الإلكتروني بشكل منفصل عن مبيعات المحل الفعلي.

---

## التغييرات في قاعدة البيانات

### 1. جدول Sales - حقول جديدة

تمت إضافة الحقول التالية لجدول `sales`:

| الحقل | النوع | الوصف |
|------|------|-------|
| `source` | text | مصدر البيع: `'store'` (المحل) أو `'salla'` (المتجر الإلكتروني) |
| `salla_order_id` | text | رقم الطلب من منصة سلة |
| `salla_shipping_cost` | decimal | تكلفة الشحن للطلبات الإلكترونية |
| `salla_payment_gateway_fee` | decimal | رسوم بوابة الدفع الإلكترونية |

**القيم الافتراضية:**
- `source`: `'store'`
- `salla_shipping_cost`: `0`
- `salla_payment_gateway_fee`: `0`

---

## Webhook Endpoint

### نقطة النهاية (Endpoint)
```
POST https://YOUR_SUPABASE_URL/functions/v1/salla-webhook
```

### الرؤوس المطلوبة (Headers)
```
Authorization: Bearer YOUR_SUPABASE_ANON_KEY
Content-Type: application/json
```

### الأحداث المدعومة
- `order.created` - عند إنشاء طلب جديد
- `order.updated` - عند تحديث طلب موجود

### ما يقوم به Webhook
1. **استقبال الطلب** من منصة سلة
2. **التحقق من التكرار** - يتجنب إنشاء نفس الطلب مرتين
3. **إدارة العملاء**:
   - البحث عن العميل برقم الجوال
   - إنشاء عميل جديد إذا لم يكن موجوداً
4. **إنشاء سجل البيع**:
   - حفظ بيانات الطلب مع `source = 'salla'`
   - حفظ تكاليف الشحن ورسوم الدفع
   - ربط الطلب برقم سلة
5. **معالجة المنتجات**:
   - البحث عن المنتجات بالاسم
   - إنشاء عناصر البيع
   - تحديث المخزون تلقائياً
6. **تحديث النقاط**:
   - حساب نقاط الولاء للعميل
   - تحديث إجمالي مشتريات العميل

### مثال على Payload من سلة
```json
{
  "event": "order.created",
  "merchant": 12345,
  "data": {
    "id": 98765,
    "reference_id": "ORD-123",
    "customer": {
      "name": "أحمد محمد",
      "mobile": "+966501234567",
      "email": "customer@example.com"
    },
    "amounts": {
      "total": 250.00,
      "subtotal": 200.00,
      "shipping_cost": 35.00,
      "payment_fee": 5.00,
      "tax": 10.00
    },
    "items": [
      {
        "name": "منتج تجريبي",
        "quantity": 2,
        "price": 100.00,
        "total": 200.00
      }
    ]
  }
}
```

---

## واجهة المستخدم

### 1. مكون المبيعات (Sales Component)

#### اختيار مصدر البيع
عند إنشاء فاتورة جديدة، يظهر حقل اختيار المصدر:
- 🏪 **مبيعات المحل** (القيمة الافتراضية)
- 🛒 **مبيعات المتجر الإلكتروني (سلة)**

#### حقول إضافية لمبيعات سلة
عند اختيار "سلة" كمصدر، تظهر حقول إضافية:
- **تكلفة الشحن**: رسوم توصيل الطلب
- **رسوم بوابة الدفع**: عمولة بوابة الدفع الإلكترونية

**ملاحظة:** هذه التكاليف تُخصم من صافي الربح تلقائياً.

---

### 2. لوحة التحكم (Dashboard)

تم تحديث لوحة التحكم لعرض 6 بطاقات بدلاً من 4:

#### البطاقات الرئيسية
1. **🏪 مبيعات المحل** (Store Sales)
   - عرض إجمالي مبيعات المحل الفعلي
   - لون تيل

2. **🛒 مبيعات المتجر الإلكتروني** (Online Sales)
   - عرض إجمالي مبيعات سلة
   - لون أزرق

3. **💰 إجمالي الإيرادات** (Total Revenue)
   - مجموع مبيعات المحل + مبيعات سلة
   - لون أخضر

4. **إجمالي المشتريات** (Total Purchases)
   - كما هو - لون أحمر

5. **صافي الربح** (Net Profit)
   - يشمل كل المبيعات مطروحاً منها:
     * المشتريات
     * المصاريف التشغيلية
     * تكاليف شحن سلة
     * رسوم بوابة دفع سلة
   - لون بنفسجي

6. **قيمة المخزون** (Inventory Value)
   - كما هو - لون برتقالي

---

### 3. التقارير (Reports)

تم تحديث قسم التقارير لعرض البيانات منفصلة:

#### البطاقات الإحصائية
1. **🏪 مبيعات المحل**
   - إجمالي المبيعات المحلية خلال الفترة المحددة

2. **🛒 مبيعات المتجر الإلكتروني**
   - إجمالي مبيعات سلة خلال الفترة المحددة

3. **💰 إجمالي الإيرادات**
   - مجموع المبيعات من كل المصادر

4. **إجمالي المشتريات**
5. **صافي الربح**
6. **متوسط البيع**

---

## حساب أرباح الشركاء

### المعادلة المحدثة
```
صافي الربح = إجمالي الإيرادات
            - إجمالي المشتريات
            - المصاريف التشغيلية
            - تكاليف شحن سلة
            - رسوم بوابة دفع سلة
```

### توزيع الأرباح
- يتم حساب حصة كل شريك من صافي الربح النهائي
- التكاليف الإضافية لسلة تُخصم قبل التوزيع
- النسب المئوية تُطبق على الربح بعد خصم جميع التكاليف

**مثال:**
- إجمالي مبيعات المحل: 10,000 ر.س
- إجمالي مبيعات سلة: 5,000 ر.س
- تكاليف شحن سلة: 500 ر.س
- رسوم بوابة دفع: 100 ر.س
- إجمالي المشتريات: 8,000 ر.س
- المصاريف التشغيلية: 1,000 ر.س

```
صافي الربح = (10,000 + 5,000) - 8,000 - 1,000 - 500 - 100
           = 15,000 - 9,600
           = 5,400 ر.س
```

إذا كان نصيب شريك 30%:
```
حصة الشريك = 5,400 × 30% = 1,620 ر.س
```

---

## إعداد Webhook في منصة سلة

### الخطوات
1. **تسجيل الدخول** إلى لوحة تحكم سلة
2. **الإعدادات** → **تطبيقات المطور** → **Webhooks**
3. **إضافة Webhook جديد**
4. **إدخال البيانات**:
   ```
   URL: https://YOUR_SUPABASE_URL/functions/v1/salla-webhook
   Events: order.created, order.updated
   Status: Active
   ```
5. **حفظ** الإعدادات

### اختبار Webhook
يمكن اختبار الـ webhook من لوحة تحكم سلة:
- اذهب إلى صفحة Webhooks
- اضغط على "Test" بجانب webhook الخاص بك
- تحقق من استجابة النظام

---

## الملفات المحدثة

### 1. Database Migration
- `supabase/migrations/add_sales_source_and_salla_fields.sql`
  - إضافة حقل `source` مع constraint
  - إضافة حقول سلة الإضافية
  - إنشاء indexes للأداء

### 2. Edge Function
- `supabase/functions/salla-webhook/index.ts`
  - معالجة طلبات سلة
  - إدارة العملاء والمنتجات
  - تحديث المخزون

### 3. Frontend Components
- `src/components/Sales.tsx`
  - إضافة حقل اختيار المصدر
  - حقول تكاليف سلة الإضافية
  - حفظ بيانات سلة

- `src/components/Dashboard.tsx`
  - فصل مبيعات المحل والمتجر الإلكتروني
  - 6 بطاقات إحصائية
  - عرض إجمالي الإيرادات

- `src/components/Reports.tsx`
  - تقارير منفصلة لكل مصدر
  - حساب إجمالي الإيرادات

- `src/components/Partners.tsx`
  - خصم تكاليف سلة من الأرباح
  - حساب دقيق لحصص الشركاء

---

## الميزات الرئيسية

✅ **فصل تام** بين مبيعات المحل ومبيعات المتجر الإلكتروني
✅ **تتبع دقيق** لتكاليف الشحن ورسوم بوابة الدفع
✅ **حساب أرباح صحيح** يخصم جميع التكاليف
✅ **تحديث تلقائي** للمخزون من طلبات سلة
✅ **إدارة العملاء** المشتركين بين القنوات
✅ **تقارير شاملة** بإجمالي الإيرادات
✅ **واجهة عربية** واضحة ومميزة

---

## ملاحظات مهمة

1. **الأمان**: Webhook endpoint لا يتطلب JWT verification لأنه نقطة استقبال عامة
2. **التكرار**: النظام يتحقق من `salla_order_id` لتجنب تكرار الطلبات
3. **المنتجات**: يتم مطابقة المنتجات بالاسم - تأكد من توافق الأسماء
4. **المخزون**: يتم تحديث الكميات تلقائياً عند استقبال الطلب
5. **العملاء**: العملاء الجدد يُضافون تلقائياً بناءً على بيانات سلة

---

## الدعم والصيانة

### مراقبة Logs
تحقق من logs في Supabase:
```bash
# في لوحة تحكم Supabase
Edge Functions → salla-webhook → Logs
```

### الأخطاء الشائعة
- **"Product not found"**: المنتج غير موجود بنفس الاسم في النظام
- **"Order already processed"**: الطلب تم معالجته مسبقاً
- **خطأ في المخزون**: تحقق من كمية المخزون المتاحة

---

## تحديثات مستقبلية محتملة

- 📊 رسوم بيانية مقارنة بين المصدرين
- 📧 إشعارات عند استقبال طلبات جديدة
- 🔄 مزامنة المخزون مع سلة
- 📦 تتبع حالة الشحن
- 💳 تقارير رسوم بوابات الدفع المختلفة

---

تاريخ التحديث: 2026-02-13
الإصدار: 1.0.0
