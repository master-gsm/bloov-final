# قائمة اختبار الـ Migrations وحماية البيانات
# Migration Testing & Data Protection Checklist

## 📋 قبل كتابة الـ Migration | Before Writing Migration

### تخطيط التغيير
- [ ] تم توثيق الهدف من التغيير بوضوح
- [ ] تم تحديد الجداول والأعمدة المتأثرة
- [ ] تم تقييم تأثير التغيير على البيانات الموجودة
- [ ] تم التخطيط لكيفية معالجة السجلات القديمة
- [ ] تم مراجعة البروتوكول الموجود في `DATA_INTEGRITY_PROTOCOL.md`

---

## ✍️ أثناء كتابة الـ Migration | While Writing Migration

### فحص الأمان الأساسي
- [ ] **لا يوجد** `DROP TABLE`
- [ ] **لا يوجد** `DROP COLUMN`
- [ ] **لا يوجد** `DELETE FROM` (إلا للبيانات التجريبية فقط)
- [ ] **لا يوجد** `TRUNCATE`
- [ ] **لا يوجد** `ALTER COLUMN TYPE` بدون اختبار دقيق

### استخدام الحمايات
- [ ] استخدام `IF NOT EXISTS` عند إنشاء جداول
- [ ] استخدام `IF EXISTS` عند التحقق من وجود أعمدة
- [ ] استخدام `DO $$ ... END $$;` blocks للعمليات المشروطة
- [ ] استخدام `CREATE INDEX IF NOT EXISTS`

### القيم الافتراضية
- [ ] كل عمود جديد له `DEFAULT` value مناسب
- [ ] تم تحديث السجلات القديمة بالقيم الافتراضية
- [ ] تم اختبار القيم الافتراضية مع أنواع البيانات المختلفة
- [ ] تم توثيق القيم الافتراضية في `COMMENT ON COLUMN`

### التعليقات والتوثيق
- [ ] رأس الـ migration يحتوي على وصف شامل
- [ ] كل عمود جديد له `COMMENT` توضيحي
- [ ] تم توضيح كيفية التعامل مع السجلات القديمة
- [ ] تم كتابة ملاحظات للمطورين المستقبليين

---

## 🧪 الاختبار المحلي | Local Testing

### اختبارات البيانات الأساسية
- [ ] تم تطبيق الـ migration على قاعدة بيانات محلية
- [ ] جميع الجداول المتأثرة لا تزال موجودة
- [ ] عدد السجلات لم يتغير (إلا إذا كان مقصوداً)
- [ ] لا توجد أخطاء SQL

### اختبار البيانات القديمة
- [ ] إنشاء سجلات اختبار "قديمة" (بدون الحقول الجديدة)
- [ ] تطبيق الـ migration
- [ ] التحقق من أن السجلات القديمة:
  - [ ] لا تزال موجودة
  - [ ] القيم الافتراضية مطبقة عليها
  - [ ] يمكن قراءتها بدون أخطاء
  - [ ] تظهر في الاستعلامات العادية

### اختبار البيانات الجديدة
- [ ] إنشاء سجلات جديدة بعد الـ migration
- [ ] التحقق من أن الحقول الجديدة:
  - [ ] تقبل القيم الصحيحة
  - [ ] القيود (constraints) تعمل بشكل صحيح
  - [ ] الفهارس (indexes) تعمل بشكل صحيح

---

## 🎨 اختبار الواجهة | UI Testing

### عرض البيانات القديمة
- [ ] الصفحات تعرض السجلات القديمة بشكل صحيح
- [ ] الحقول الناقصة تعرض رسائل واضحة ("N/A" أو "غير متوفر")
- [ ] لا توجد أخطاء JavaScript في console
- [ ] الصور/المرفقات المفقودة تعرض placeholder مناسب

### الفلاتر والبحث
- [ ] الفلاتر لا تخفي السجلات القديمة بالخطأ
- [ ] البحث يعمل على السجلات القديمة والجديدة
- [ ] الترتيب (sorting) لا يسبب أخطاء مع البيانات الناقصة
- [ ] التصفح بين الصفحات (pagination) يعمل بشكل صحيح

### التقارير
- [ ] التقارير تشمل البيانات القديمة
- [ ] الحسابات صحيحة مع البيانات القديمة
- [ ] التصدير (Excel/PDF) يعمل مع البيانات المختلطة
- [ ] الرسوم البيانية تعرض البيانات بشكل صحيح

---

## 🔍 اختبارات SQL المتقدمة | Advanced SQL Tests

### التحقق من عدد السجلات
```sql
-- قبل Migration
SELECT 'sales' as table_name, COUNT(*) as count_before FROM sales
UNION ALL
SELECT 'purchases', COUNT(*) FROM purchases
UNION ALL
SELECT 'products', COUNT(*) FROM products;

-- بعد Migration
-- يجب أن تكون الأرقام متطابقة!
```
- [ ] تم تشغيل الاستعلام وتسجيل النتائج

### التحقق من القيم NULL
```sql
-- فحص الأعمدة الجديدة
SELECT
  COUNT(*) FILTER (WHERE new_column IS NULL) as null_count,
  COUNT(*) as total_count,
  ROUND((COUNT(*) FILTER (WHERE new_column IS NULL)::numeric / COUNT(*) * 100), 2) as null_percentage
FROM your_table;
```
- [ ] لا توجد قيم NULL في الأعمدة المطلوبة
- [ ] القيم الافتراضية مطبقة على جميع السجلات

### فحص الأداء
```sql
-- قياس سرعة الاستعلامات المهمة
EXPLAIN ANALYZE
SELECT * FROM sales WHERE branch_id = 'xxx' LIMIT 100;
```
- [ ] الاستعلامات لا تزال سريعة
- [ ] الفهارس تستخدم بشكل صحيح
- [ ] لا توجد full table scans غير ضرورية

### اختبار Rollback
```sql
-- إذا أمكن، اختبر التراجع عن الـ migration
BEGIN;
  -- تطبيق migration
  -- اختبار
ROLLBACK; -- أو COMMIT إذا كان كل شيء صحيح
```
- [ ] تم اختبار إمكانية التراجع
- [ ] البيانات لم تتضرر أثناء الاختبار

---

## 📊 اختبار السيناريوهات الحقيقية | Real Scenario Testing

### سيناريو 1: مبيعات قديمة
- [ ] فتح صفحة المبيعات
- [ ] البحث عن مبيعات من شهر مضى
- [ ] التحقق من عرض جميع البيانات بشكل صحيح
- [ ] تصدير التقرير والتحقق من وجود البيانات القديمة

### سيناريو 2: مشتريات قديمة
- [ ] فتح صفحة المشتريات
- [ ] البحث عن مشتريات قديمة
- [ ] التحقق من عرض الموردين بشكل صحيح
- [ ] تعديل مشترى قديم (إذا كانت الصلاحيات تسمح)

### سيناريو 3: منتجات قديمة
- [ ] فتح صفحة المخزون
- [ ] التحقق من عرض المنتجات القديمة
- [ ] التحقق من حساب الكميات بشكل صحيح
- [ ] إضافة كمية لمنتج قديم

### سيناريو 4: تقرير شامل
- [ ] فتح صفحة التقارير
- [ ] اختيار فترة زمنية تشمل بيانات قديمة وجديدة
- [ ] التحقق من دقة الأرقام
- [ ] تصدير Excel والتحقق من البيانات

---

## 🚀 قبل الـ Deploy | Before Deploy

### مراجعة نهائية
- [ ] مراجعة كود الـ migration بواسطة مطور آخر
- [ ] التأكد من وجود نسخة احتياطية حديثة
- [ ] التأكد من إمكانية الرجوع إلى النسخة السابقة
- [ ] توثيق التغييرات في changelog

### اختبار Production-like
- [ ] الاختبار على قاعدة بيانات مشابهة للإنتاج
- [ ] الاختبار مع حجم بيانات مشابه
- [ ] قياس وقت تطبيق الـ migration

### خطة الطوارئ
- [ ] تحضير سكريبت rollback إذا لزم الأمر
- [ ] تجهيز خطة استعادة من النسخة الاحتياطية
- [ ] إعلام الفريق بموعد التحديث
- [ ] تحديد نافذة زمنية للصيانة إذا لزم الأمر

---

## ✅ بعد الـ Deploy | After Deploy

### مراقبة فورية (أول 15 دقيقة)
- [ ] التحقق من نجاح تطبيق الـ migration
- [ ] مراقبة logs للأخطاء
- [ ] اختبار الصفحات الرئيسية
- [ ] التحقق من عمل API endpoints

### اختبار شامل (أول ساعة)
- [ ] اختبار جميع الصفحات المتأثرة
- [ ] التحقق من التقارير
- [ ] اختبار العمليات الحرجة (بيع، شراء، إلخ)
- [ ] التحقق من البيانات القديمة لا تزال تظهر

### مراقبة مستمرة (أول 24 ساعة)
- [ ] مراقبة أداء قاعدة البيانات
- [ ] متابعة شكاوى المستخدمين
- [ ] التحقق من عدم وجود بطء غير طبيعي
- [ ] فحص logs بشكل دوري

---

## 🔧 أدوات مساعدة | Helper Tools

### سكريبت فحص سريع
```sql
-- نسخ هذا السكريبت وتشغيله بعد كل migration
DO $$
DECLARE
  v_migration_name text := 'your_migration_name';
BEGIN
  RAISE NOTICE 'Testing migration: %', v_migration_name;

  -- Test 1: Count records
  RAISE NOTICE 'Total sales: %', (SELECT COUNT(*) FROM sales);
  RAISE NOTICE 'Total products: %', (SELECT COUNT(*) FROM products);

  -- Test 2: Check nulls
  RAISE NOTICE 'Sales with null branch: %',
    (SELECT COUNT(*) FROM sales WHERE branch_id IS NULL);

  -- Test 3: Check recent data
  RAISE NOTICE 'Sales last 7 days: %',
    (SELECT COUNT(*) FROM sales WHERE created_at > NOW() - INTERVAL '7 days');

  RAISE NOTICE 'Migration test completed!';
END $$;
```

### TypeScript Type Check
```typescript
// تشغيل هذا قبل الـ deploy
npm run typecheck

// يجب ألا يكون هناك أخطاء
```

### Build Test
```bash
# تأكد من أن المشروع يبني بنجاح
npm run build

# يجب أن ينتهي بدون أخطاء
```

---

## 📝 Template لملف الاختبار

```markdown
# اختبار Migration: [اسم الـ Migration]

## التاريخ: YYYY-MM-DD
## المسؤول: [اسمك]

### ملخص التغيير:
[وصف موجز للتغيير]

### الجداول المتأثرة:
- [ ] sales
- [ ] products
- [ ] ...

### نتائج الاختبارات:

#### قبل التطبيق:
- عدد سجلات sales: XXX
- عدد سجلات products: XXX

#### بعد التطبيق:
- عدد سجلات sales: XXX (✅ متطابق)
- عدد سجلات products: XXX (✅ متطابق)

#### اختبارات الواجهة:
- [✅/❌] البيانات القديمة تظهر
- [✅/❌] التقارير تعمل
- [✅/❌] لا توجد أخطاء في console

#### ملاحظات:
[أي ملاحظات أو مشاكل واجهتها]

### التوقيع:
اختبر بواسطة: [اسمك]
معتمد من: [اسم المراجع]
```

---

## 🎯 معايير النجاح | Success Criteria

Migration يعتبر ناجح فقط إذا:

### ✅ المعايير الأساسية:
1. ✅ جميع السجلات القديمة لا تزال موجودة
2. ✅ لا توجد أخطاء SQL
3. ✅ القيم الافتراضية مطبقة بشكل صحيح
4. ✅ الواجهة تعرض البيانات بشكل صحيح

### ✅ معايير الأداء:
5. ✅ الاستعلامات لا تزال سريعة
6. ✅ لا يوجد تأثير سلبي على المستخدمين
7. ✅ النسخ الاحتياطية تعمل

### ✅ معايير الجودة:
8. ✅ الكود موثق بشكل جيد
9. ✅ التغييرات مراجعة من مطور آخر
10. ✅ الاختبارات شاملة ومو ثقة

---

## ⚠️ علامات تحذير | Warning Signs

احذر إذا لاحظت:
- ⚠️ عدد السجلات انخفض بشكل غير متوقع
- ⚠️ الاستعلامات أصبحت أبطأ بشكل ملحوظ
- ⚠️ ظهور أخطاء في logs
- ⚠️ شكاوى من المستخدمين عن بيانات مفقودة
- ⚠️ التقارير تعطي نتائج غريبة

**في حالة أي من هذه العلامات: توقف فوراً واتصل بالفريق التقني!**

---

## 📞 جهات الاتصال للطوارئ

في حالة مشكلة خطيرة:
1. أوقف التحديث إذا كان لا يزال جارياً
2. اتصل بـ: [اسم مسؤول قاعدة البيانات]
3. راجع النسخة الاحتياطية الأخيرة
4. وثق المشكلة بالتفصيل

---

**تذكر**: البيانات أثمن من الميزات الجديدة!
**Remember**: Data is more valuable than new features!

---

**آخر تحديث**: 2026-02-15
**الإصدار**: 1.0
