# دليل نظام إدارة الفروع المتعددة

## نظرة عامة
يدعم نظام BLOOV الآن نظام إدارة فروع متعدد شامل يمكّنك من تشغيل مواقع متاجر متعددة مع عزل صارم للبيانات مع الحفاظ على الرقابة المركزية للمديرين.

## الميزات الرئيسية

### 1. إدارة الفروع
- **إنشاء وإدارة فروع متعددة**: إضافة فروع غير محدودة مع رموز فريدة ومواقع ومديرين
- **تفاصيل الفروع**: تتبع اسم الفرع والرمز والموقع والمدينة والهاتف والمدير وتاريخ الافتتاح والحالة التشغيلية
- **حالة نشط/غير نشط**: التحكم في الفروع التشغيلية

### 2. عزل صارم للبيانات
جميع بيانات الأعمال الآن خاصة بالفرع لضمان الفصل الكامل:
- **المبيعات والفواتير**: كل فرع يمكنه فقط رؤية وإدارة مبيعاته الخاصة
- **المخزون**: كتالوج منتجات عالمي مع مستويات مخزون خاصة بالفرع
- **المصاريف**: المصاريف التشغيلية معزولة لكل فرع
- **إدارة النقد**: الصناديق والمعاملات النقدية خاصة بالفرع
- **المشتريات**: سجلات المشتريات منفصلة حسب الفرع

### 3. البيانات العامة مقابل البيانات الخاصة بالفرع
- **كتالوج المنتجات العالمي**: المنتجات مشتركة بين جميع الفروع
- **المخزون الخاص بالفرع**: كل فرع يحتفظ بكميات المخزون الخاصة به
- **العملاء العالميون**: قاعدة بيانات العملاء مشتركة، ولكن تتبع فرع الأصل
- **الموردون المشتركون**: قاعدة بيانات الموردين متاحة لجميع الفروع

### 4. أدوار المستخدمين وتعيين الفرع
- **تعيين الفرع**: يتم تعيين كل مستخدم لفرع محدد
- **الوصول حسب الدور**: يمكن للمستخدمين الوصول فقط إلى البيانات من الفرع المعين لهم
- **دور المدير العام**: دور جديد مع الوصول إلى جميع الفروع للرقابة المركزية

### 5. مصاريف التأسيس (المصاريف الرأسمالية)
وحدة مخصصة لتتبع مصاريف التأسيس والرأسمالية:
- **منفصلة عن المصاريف التشغيلية**: يتم تتبع مصاريف التأسيس بشكل مستقل
- **خاصة بالفرع أو عامة**: يمكن تعيينها لفرع محدد أو وضع علامة عليها كعامة (على مستوى الشركة)
- **دعم الإطفاء**: خيار لإطفاء المصاريف على مدى عدة أشهر
- **الفئات**: الأثاث، المعدات، التجديد، التراخيص، التكنولوجيا، إلخ
- **حساب العائد على الاستثمار**: يساعد في حساب نقطة التعادل والعائد على الاستثمار

### 6. إدارة المخزون الخاص بالفرع
جدول `branch_stock` الجديد يوفر:
- كميات المخزون الخاصة بالفرع
- الحد الأدنى/الأقصى لمستويات المخزون لكل فرع
- تتبع تاريخ آخر تجديد للمخزون
- تنبيهات المخزون المنخفض لكل فرع
- تقييم المخزون حسب الفرع

## تغييرات قاعدة البيانات

### جداول جديدة

#### 1. `branches` (الفروع)
تخزين جميع معلومات الفروع:
- `id`: معرف فريد للفرع
- `name`: اسم الفرع (مثل "المتجر الرئيسي")
- `code`: رمز فريد قصير (مثل "MAIN")
- `location`: العنوان الفعلي
- `city`: اسم المدينة
- `phone`: رقم هاتف الفرع
- `manager_id`: المستخدم المعين كمدير الفرع
- `is_active`: ما إذا كان الفرع تشغيليًا
- `opening_date`: تاريخ افتتاح الفرع
- `metadata`: إعدادات إضافية للفرع (JSON)

#### 2. `branch_stock` (مخزون الفرع)
تتبع المخزون لكل فرع:
- `branch_id`: مرجع للفرع
- `product_id`: مرجع للمنتج
- `quantity`: كمية المخزون الحالية في هذا الفرع
- `min_stock_level`: نقطة إعادة الطلب لهذا الفرع
- `max_stock_level`: الحد الأقصى لسعة المخزون
- `last_restock_date`: آخر مرة تم فيها تجديد المخزون

#### 3. `setup_expenses` (مصاريف التأسيس)
المصاريف الرأسمالية والتأسيسية:
- `branch_id`: مرجع الفرع (قابل للإلغاء للمصاريف على مستوى الشركة)
- `category`: فئة المصروف
- `description`: وصف تفصيلي
- `amount`: مبلغ المصروف
- `expense_date`: تاريخ حدوث المصروف
- `supplier_id`: مرجع المورد (إن وجد)
- `payment_method`: طريقة الدفع
- `receipt_number`: رقم الإيصال/الفاتورة
- `attachment`: رابط المرفق
- `is_amortizable`: ما إذا كان يمكن إطفاء المصروف
- `amortization_months`: عدد الأشهر للإطفاء
- `notes`: ملاحظات إضافية

### الجداول المعدلة
الجداول التالية تتضمن الآن عمود `branch_id`:
- `users`: الفرع المعين للمستخدم
- `sales`: معاملات المبيعات لكل فرع
- `inventory`: المخزون القديم (تم إيقافه لصالح branch_stock)
- `expenses`: المصاريف التشغيلية لكل فرع
- `customers`: فرع أصل العميل
- `cash_transactions`: المعاملات النقدية لكل فرع
- `purchases`: سجلات المشتريات لكل فرع
- `operating_expenses`: المصاريف التشغيلية لكل فرع
- `cash_shifts`: سجلات نوبات الصندوق لكل فرع

## أدوار المستخدمين

### دور جديد: المدير العام (Super Admin)
- وصول كامل إلى جميع الفروع
- يمكنه عرض التقارير الموحدة عبر جميع الفروع
- يمكنه التبديل بين طرق عرض الفروع للتحليل التفصيلي
- يدير إنشاء الفروع وتكوينها

### الأدوار الموجودة (مقيدة الآن بالفرع)
- **مدير (Admin)**: وصول كامل إلى الفرع المعين فقط
- **مشرف (Manager)**: يدير العمليات في الفرع المعين
- **موظف (Employee)**: وصول محدود إلى فرعه
- **محاسب (Accountant)**: وصول مالي إلى فرعه
- **مراقب (Observer)**: وصول للقراءة فقط إلى فرعه

## كيفية الاستخدام

### للمديرين العامين

#### إنشاء فرع جديد
1. انتقل إلى "الفروع" من الشريط الجانبي
2. انقر على "إضافة فرع"
3. املأ:
   - اسم الفرع (مثل "فرع المول")
   - رمز الفرع (فريد، مثل "MALL1")
   - الموقع والمدينة
   - رقم الهاتف
   - تعيين مدير الفرع
   - تحديد تاريخ الافتتاح
   - وضع علامة نشط/غير نشط
4. حفظ

#### تعيين المستخدمين للفروع
1. انتقل إلى "إدارة المستخدمين"
2. عند إنشاء أو تعديل مستخدم، حدد تعيين الفرع الخاص به
3. سيتم تقييد المستخدمين تلقائيًا ببيانات الفرع المعين لهم

#### عرض التقارير الموحدة
كمدير عام، يمكنك:
- استخدام دالة `get_consolidated_sales_summary()` لرؤية المبيعات عبر جميع الفروع
- استخدام دالة `get_branch_stock_summary()` لعرض المخزون عبر جميع الفروع
- التبديل بين طرق عرض الفروع في لوحة التحكم (تحسين مستقبلي)

#### إدارة مصاريف التأسيس
1. انتقل إلى "مصاريف التأسيس" من الشريط الجانبي
2. انقر على "إضافة مصروف"
3. حدد:
   - الفئة (الأثاث، المعدات، التجديد، إلخ)
   - الفرع (أو اتركه فارغًا للمصاريف العامة)
   - الوصف والمبلغ
   - التاريخ وتفاصيل الدفع
   - تفعيل الإطفاء إذا لزم الأمر (مع عدد الأشهر)
4. عرض إجمالي مصاريف التأسيس وفصلها عن المصاريف التشغيلية

### لمستخدمي الفروع

#### العمل في فرعك
- بعد تسجيل الدخول، سترى تلقائيًا فقط البيانات من الفرع المعين لك
- جميع المبيعات والمشتريات والمخزون والمصاريف مفلترة إلى فرعك
- لا يمكنك الوصول إلى البيانات من الفروع الأخرى

#### إدارة مخزون الفرع
1. انتقل إلى "المخزون"
2. عرض وإدارة مستويات المخزون لفرعك فقط
3. تعيين الحد الأدنى/الأقصى لمستويات المخزون الخاصة بالفرع
4. تلقي التنبيهات عندما ينخفض المخزون في فرعك

#### تسجيل مصاريف الفرع
1. انتقل إلى "المصاريف التشغيلية"
2. يتم وضع علامة على جميع المصاريف تلقائيًا لفرعك
3. عرض سجل المصاريف لفرعك فقط

## ترحيل البيانات

تم تعيين جميع البيانات الموجودة تلقائيًا إلى "الفرع الرئيسي" الافتراضي:
- رمز الفرع: "MAIN"
- تعيين جميع المستخدمين الحاليين للفرع الرئيسي
- تعيين جميع المبيعات والمخزون والمصاريف للفرع الرئيسي
- إنشاء إدخالات المخزون الخاصة بالفرع من المخزون الموجود

## الأمان والعزل

### سياسات أمان مستوى الصف (RLS)
يفرض النظام عزلًا صارمًا للبيانات من خلال PostgreSQL RLS:
- يمكن للمستخدمين فقط SELECT البيانات من الفرع المعين لهم
- يمكن للمستخدمين فقط INSERT/UPDATE/DELETE البيانات في الفرع المعين لهم
- المديرون العامون يتجاوزون هذه القيود
- جدول العملاء يبقى متاحًا عالميًا (مع تتبع فرع الأصل)
- جدول المنتجات يبقى متاحًا عالميًا

### الدوال المساعدة
- `is_super_admin()`: التحقق مما إذا كان المستخدم الحالي مديرًا عامًا
- `get_user_branch_id()`: الحصول على معرف فرع المستخدم الحالي
- `get_consolidated_sales_summary()`: الحصول على ملخص المبيعات عبر جميع الفروع (مدير عام فقط)
- `get_branch_stock_summary()`: الحصول على ملخص المخزون حسب الفرع

## التقارير والتحليلات

### مقارنة أداء الفروع (مدير عام)
يمكن للمديرين العامين مقارنة:
- إجمالي المبيعات حسب الفرع
- متوسط قيمة الطلب حسب الفرع
- مستويات المخزون حسب الفرع
- نسب المصاريف حسب الفرع
- الربحية حسب الفرع

### مصاريف التأسيس مقابل المصاريف التشغيلية
النظام الآن يميز بين:
- **مصاريف التأسيس (CapEx)**: المصاريف الرأسمالية والتأسيسية لمرة واحدة
  - تستخدم لحسابات العائد على الاستثمار ونقطة التعادل
  - يمكن إطفاؤها بمرور الوقت
  - يتم تتبعها بشكل منفصل لكل فرع أو على مستوى الشركة

- **المصاريف التشغيلية (OpEx)**: التكاليف التشغيلية اليومية المتكررة
  - يتم تتبعها لكل فرع
  - تستخدم لتحليل الربحية المستمرة

## أفضل الممارسات

1. **رموز الفروع**: استخدم رموزًا قصيرة لا تُنسى (مثل MAIN، MALL1، DOWNTOWN)
2. **تعيين المدير**: عيّن دائمًا مديرًا لكل فرع للمساءلة
3. **نقل المخزون المنتظم**: عند نقل المخزون بين الفروع، سجله بشكل صحيح
4. **تصنيف مصاريف التأسيس**: كن واضحًا بشأن ما يشكل CapEx مقابل OpEx
5. **إعدادات خاصة بالفرع**: حافظ على معلومات اتصال دقيقة للفرع
6. **وصول المدير العام**: قصر دور المدير العام على الملاك والإدارة العليا فقط

## التحسينات المستقبلية

الإضافات المحتملة لنظام الفروع المتعددة:
- نقل المخزون بين الفروع
- طلبات من فرع إلى فرع
- البيانات المالية الموحدة
- لوحات معلومات أداء الفروع
- أدوات إعداد التقارير متعددة الفروع
- قواعد التسعير الخاصة بالفرع
- إدارة المستودعات المركزية
- حسابات تقاسم الأرباح بين الفروع

## استكشاف الأخطاء

### المستخدمون لا يمكنهم رؤية البيانات
- تحقق من تعيين المستخدم للفرع الصحيح
- تحقق مما إذا كان الفرع مُعلَّمًا كنشط
- تأكد من أن المستخدم لديه أذونات الدور الصحيحة

### المخزون لا يظهر
- تأكد من وجود إدخالات branch_stock للمنتجات
- تحقق مما إذا تم ترحيل المخزون من جدول المخزون القديم
- تحقق من أن الفرع نشط

### لا يمكن الوصول إلى مصاريف التأسيس
- فقط أدوار admin وsuper_admin يمكنها الوصول إلى مصاريف التأسيس
- تحقق من تعيين دور المستخدم

### أخطاء سياسة RLS
- تحقق من أن المستخدم لديه branch_id صالح معين
- تحقق مما إذا كان المستخدم مصادقًا عليه
- تأكد من وجود الفرع ونشاطه

## الدعم

للحصول على مساعدة إضافية أو أسئلة حول نظام الفروع المتعددة، يرجى الرجوع إلى ملفات ترحيل قاعدة البيانات في `supabase/migrations/` أو الاتصال بمسؤول النظام.
