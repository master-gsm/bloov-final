# دليل النسخ الاحتياطي والاستعادة
# Backup & Restore Guide

---

## 🔄 نظام النسخ الاحتياطي الكامل

تم إضافة نظام نسخ احتياطي كامل لحماية بياناتك واستعادتها في حالة الحاجة.

---

## 📋 المحتويات

1. [حول البرنامج](#حول-البرنامج)
2. [النسخ الاحتياطي اليدوي](#النسخ-الاحتياطي-اليدوي)
3. [النسخ الاحتياطي التلقائي](#النسخ-الاحتياطي-التلقائي)
4. [استعادة النسخة الاحتياطية](#استعادة-النسخة-الاحتياطية)
5. [الأسئلة الشائعة](#الأسئلة-الشائعة)

---

## 🌐 حول البرنامج

### هل البرنامج يعمل أوفلاين أم أونلاين؟

**البرنامج حالياً يعمل أونلاين فقط** ويتطلب اتصال بالإنترنت.

**السبب:**
- جميع البيانات محفوظة في قاعدة بيانات Supabase السحابية
- هذا يضمن أمان البيانات ونسخها احتياطياً تلقائياً على السحابة
- يمكنك الوصول لبياناتك من أي جهاز بنفس الحساب

**الحل للعمل محلياً:**
- استخدم نظام النسخ الاحتياطي لحفظ نسخة محلية من بياناتك
- يمكنك استعادة البيانات في أي وقت
- النسخ الاحتياطي التلقائي يحفظ بياناتك كل دقيقة (أو حسب الإعداد)

---

## 💾 النسخ الاحتياطي اليدوي

### كيفية إنشاء نسخة احتياطية يدوياً:

1. **افتح الإعدادات**
   - اذهب إلى قائمة "الإعدادات"
   - اختر تبويب "النسخ الاحتياطي"

2. **اختر نوع النسخة:**

   **نسخة JSON (موصى بها للاستعادة):**
   - اضغط على "تحميل (JSON)"
   - هذه النسخة كاملة وقابلة للاستعادة
   - تحتوي على جميع البيانات بصيغتها الأصلية
   - **الاستخدام:** للنسخ الاحتياطي والاستعادة

   **نسخة Excel/CSV (للاطلاع فقط):**
   - اضغط على "تحميل (Excel/CSV)"
   - هذه نسخة للاطلاع على البيانات فقط
   - لا يمكن استعادتها في البرنامج
   - **الاستخدام:** لعرض البيانات في Excel أو Google Sheets

3. **احفظ الملف:**
   - سيتم تحميل الملف على جهازك
   - احفظه في مكان آمن
   - اسم الملف: `bloov-backup-YYYY-MM-DD.json` أو `.csv`

---

## ⚡ النسخ الاحتياطي التلقائي

### إعداد النسخ الاحتياطي التلقائي:

1. **افتح الإعدادات**
   - اذهب إلى "الإعدادات" → "النسخ الاحتياطي"

2. **فعّل النسخ التلقائي:**
   - فعّل "تفعيل النسخ التلقائي"

3. **حدد الفترة:**
   - **فترة النسخ (بالدقائق)**
   - الحد الأدنى الموصى به: **5 دقائق**
   - للنسخ كل دقيقة: أدخل **1** (قد يؤثر على الأداء)
   - للنسخ كل ساعة: أدخل **60**

4. **حدد مسار الحفظ:**
   - أدخل المسار على جهازك (مثل: `C:/BloovBackups`)
   - سيتم حفظ النسخ في هذا المجلد

5. **اختر طريقة الحفظ:**

   **الحفظ على القرص الصلب:**
   - فعّل "الحفظ على القرص الصلب"
   - سيتم حفظ النسخة في المسار المحدد
   - **يتطلب تطبيق Electron (سطح المكتب)**

   **الحفظ في المتصفح:**
   - فعّل "الحفظ في المتصفح"
   - سيتم حفظ النسخة في ذاكرة المتصفح (localStorage)
   - محدود بحجم المتصفح (عادة 5-10 MB)

6. **احفظ الإعدادات:**
   - اضغط "حفظ الإعدادات" في أعلى الصفحة

### كيف يعمل النسخ التلقائي:

- **يبدأ فوراً** بعد حفظ الإعدادات
- **ينفذ كل X دقيقة** (حسب الإعداد)
- **يحفظ تلقائياً** في المكان المحدد
- **يعرض آخر نسخة** مع التاريخ والوقت

### ملاحظات مهمة:

⚠️ **للنسخ كل دقيقة:**
- قد يؤثر على أداء الجهاز
- يستهلك مساحة أكبر
- مفيد للبيانات الحساسة جداً

✅ **الإعداد الموصى به:**
- **كل 5-15 دقيقة** للاستخدام العادي
- **كل 30-60 دقيقة** للاستخدام الخفيف

---

## 🔄 استعادة النسخة الاحتياطية

### متى تحتاج للاستعادة؟

- حذف بيانات بالخطأ
- مشكلة في قاعدة البيانات
- نقل البيانات لجهاز آخر
- استعادة نسخة قديمة

### خطوات الاستعادة:

1. **افتح الإعدادات**
   - اذهب إلى "الإعدادات" → "النسخ الاحتياطي"

2. **اضغط "اختر ملف النسخة الاحتياطية"**
   - اختر ملف JSON فقط
   - الملفات المقبولة: `.json`

3. **انتظر الاستعادة:**
   - سيتم رفع الملف
   - سيتم استعادة البيانات
   - قد يستغرق بضع ثوانٍ إلى دقيقة

4. **إعادة تحميل الصفحة:**
   - ستُعاد تحميل الصفحة تلقائياً
   - ستظهر البيانات المستعادة

### ملاحظات هامة:

⚠️ **تحذير:**
- الاستعادة **تستبدل البيانات الحالية**
- **أنشئ نسخة احتياطية أولاً** قبل الاستعادة
- لا يمكن التراجع بعد الاستعادة

⚠️ **في حالة الأخطاء:**
- إذا ظهرت أخطاء أثناء الاستعادة
- ستظهر رسالة توضح الأخطاء
- بعض البيانات قد تُستعاد والبعض الآخر لا
- جرب مرة أخرى أو استخدم نسخة أخرى

---

## ❓ الأسئلة الشائعة

### س: هل يمكنني استعادة نسخة Excel/CSV؟
**ج:** لا، نسخة Excel/CSV للاطلاع فقط. استخدم نسخة JSON للاستعادة.

### س: أين تُحفظ النسخ التلقائية؟
**ج:**
- إذا فعّلت "الحفظ على القرص الصلب": في المسار الذي حددته
- إذا فعّلت "الحفظ في المتصفح": في ذاكرة المتصفح (localStorage)

### س: كم مساحة تحتاج النسخة الاحتياطية؟
**ج:** يعتمد على كمية بياناتك:
- صغيرة (100-1000 سجل): 100 KB - 1 MB
- متوسطة (1000-10000 سجل): 1-10 MB
- كبيرة (10000+ سجل): 10+ MB

### س: هل النسخ التلقائي يعمل عند إغلاق البرنامج؟
**ج:** لا، النسخ التلقائي يعمل فقط أثناء فتح البرنامج.

### س: هل يمكن جدولة النسخ التلقائي لأوقات محددة؟
**ج:** حالياً لا، لكن يمكنك استخدام Windows Task Scheduler لفتح البرنامج وإنشاء نسخة يدوية.

### س: ماذا لو نسيت إنشاء نسخة قبل مشكلة؟
**ج:**
- إذا فعّلت النسخ التلقائي، يمكنك استعادة آخر نسخة
- إذا حفظت في المتصفح، يمكنك استعادتها من localStorage
- إذا لم تفعّل النسخ التلقائي، قد تفقد البيانات

### س: هل البيانات محمية؟
**ج:**
- نعم، جميع البيانات على السحابة محمية ومشفرة
- النسخ المحلية محمية بحماية جهازك
- احتفظ بالنسخ في مكان آمن

### س: كيف أنقل البيانات لجهاز آخر؟
**ج:**
1. أنشئ نسخة احتياطية JSON على الجهاز القديم
2. انقل الملف للجهاز الجديد
3. افتح البرنامج على الجهاز الجديد
4. استعد النسخة الاحتياطية

### س: ما الفرق بين النسخ على السحابة والنسخ المحلي؟
**ج:**
- **السحابة (Supabase):**
  - نسخ تلقائي كل فترة
  - يمكن الوصول من أي جهاز
  - محمي ومشفر
- **النسخ المحلي:**
  - تحت سيطرتك الكاملة
  - لا يحتاج إنترنت للوصول للملف
  - يمكن حفظه على أكثر من مكان

---

## 🛡️ نصائح للأمان

1. **أنشئ نسخة يومية** على الأقل
2. **احتفظ بنسخ في أماكن متعددة:**
   - على جهازك المحلي
   - على قرص خارجي
   - على السحابة (Google Drive, Dropbox)
3. **اختبر الاستعادة** بشكل دوري
4. **لا تشارك ملفات النسخ** مع أحد
5. **احذف النسخ القديمة** بعد فترة
6. **فعّل النسخ التلقائي** دائماً

---

## 📞 الدعم

إذا واجهت أي مشكلة:
- راجع هذا الدليل أولاً
- تحقق من رسائل الأخطاء
- جرب نسخة احتياطية أخرى
- تواصل مع الدعم الفني

---

**تم تحديث هذا الدليل:** 2026-02-13
**الإصدار:** 1.0.0
