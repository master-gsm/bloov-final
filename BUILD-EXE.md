# كيفية إنشاء ملف exe لنظام Windows

## خطوات سريعة لبناء التطبيق

### 1. افتح Terminal/Command Prompt في مجلد المشروع

### 2. نفذ الأمر التالي:

```bash
npm run electron:build:win
```

### 3. انتظر حتى يكتمل البناء (5-10 دقائق)

سترى رسالة:
```
• building        target=nsis arch=x64 file=release\Bloov-Accounting-Setup-1.0.0.exe
```

### 4. ستجد ملف التنصيب في:

```
release/Bloov-Accounting-Setup-1.0.0.exe
```

---

## استخدام الملف

### للتوزيع:
- انسخ ملف `Bloov-Accounting-Setup-1.0.0.exe`
- أرسله لأي شخص
- يعمل على أي جهاز Windows (64-bit)

### للتنصيب:
1. افتح الملف بـ double-click
2. اتبع خطوات التنصيب
3. اختر مسار التنصيب (اختياري)
4. سيتم إنشاء اختصار على سطح المكتب

---

## ملاحظات

### حجم الملف
- الملف سيكون حوالي 100-150 MB
- يحتوي على كل ما يحتاجه التطبيق

### متطلبات التشغيل
- Windows 7 أو أحدث
- اتصال بالإنترنت (للاتصال بقاعدة البيانات)

---

## بناء نسخة محمولة (بدون تنصيب)

إذا كنت تريد نسخة تعمل مباشرة بدون تنصيب:

```bash
npm run electron:build:win
```

ثم انسخ مجلد: `release/win-unpacked/`

يمكن نسخ هذا المجلد على أي جهاز وتشغيل `Bloov Accounting System.exe` مباشرة.
