# دليل وضع التجربة (Test Mode)

## نظرة عامة
وضع التجربة هو ميزة للمطورين تمنع حفظ البيانات في قاعدة البيانات. مثالي لتجربة الميزات دون التأثير على البيانات الحقيقية.

## المميزات

### 1. مفتاح تفعيل/إيقاف وضع التجربة
- موجود في **الإعدادات** ← تبويب **النسخ الاحتياطي**
- يظهر تنبيه برتقالي بارز عند التفعيل
- شريط التنبيه يظهر في أعلى جميع الصفحات

### 2. حماية البيانات
عند تفعيل وضع التجربة:
- لا يتم حفظ أي بيانات في قاعدة البيانات
- جميع عمليات INSERT و UPDATE و DELETE و UPSERT محظورة
- العمليات تبدو أنها نجحت لكنها لا تُكتب فعلياً
- Console يُظهر تحذيرات للعمليات المحظورة

### 3. وظيفة تنظيف البيانات
وظيفة التنظيف الآن تحذف فقط:
- جميع المبيعات والفواتير
- جميع المشتريات
- جميع حركات الصندوق
- جميع المصروفات

**لن يتم حذف:**
- المنتجات
- العملاء
- الموردين
- المخزون
- الشركاء
- التصنيفات

## كيفية الاستخدام

### تفعيل وضع التجربة
1. اذهب إلى **الإعدادات** (Settings)
2. اضغط على تبويب **النسخ الاحتياطي** (Backup & Restore)
3. ابحث عن قسم **وضع التجربة (Test Mode)**
4. فعّل المفتاح (Toggle ON)
5. ستظهر رسالة برتقالية نابضة في أعلى الشاشة

### إيقاف وضع التجربة
1. اذهب إلى **الإعدادات**
2. اضغط على تبويب **النسخ الاحتياطي**
3. ابحث عن قسم **وضع التجربة**
4. أوقف المفتاح (Toggle OFF)

### تنظيف بيانات التجربة
1. تأكد أنك مسجل دخول كـ Admin
2. اذهب إلى **الإعدادات** ← **النسخ الاحتياطي**
3. انزل إلى قسم **تنظيف بيانات التجربة (Reset Test Database)**
4. اضغط الزر الأحمر
5. اكتب **RESET** بالأحرف الكبيرة
6. اضغط تأكيد

## التنفيذ التقني

### للمطورين
إذا أردت إضافة حماية وضع التجربة لميزات جديدة:

```typescript
import { isTestModeActive } from '../lib/testModeGuard';

// قبل أي عملية حفظ
if (isTestModeActive()) {
  console.warn('🧪 Test Mode: عملية محظورة - لن يتم حفظ البيانات');
  return;
}

// تابع العملية العادية
await supabase.from('sales').insert(data);
```

### استخدام Context
```typescript
import { useTestMode } from '../contexts/TestModeContext';

function MyComponent() {
  const { isTestMode, setTestMode } = useTestMode();

  if (isTestMode) {
    console.log('وضع التجربة مفعّل');
  }
}
```

## المؤشرات المرئية

عند تفعيل وضع التجربة:
- شريط تنبيه برتقالي في الأعلى: "🧪 وضع التجربة مفعّل - لن يتم حفظ أي بيانات!"
- حركة نابضة على التنبيه (animate-pulse)
- قسم وضع التجربة في الإعدادات يُظهر الحالة النشطة

## Edge Function
دالة `reset-test-database`:
- تتطلب مصادقة Admin
- تستخدم Service Role Key لتجاوز RLS
- تسجل جميع العمليات في audit_logs
- ترجع إحصائيات مفصّلة عن الحذف

## أفضل الممارسات
1. دائماً فعّل وضع التجربة عند اختبار ميزات جديدة
2. نظّف بيانات التجربة بانتظام باستخدام وظيفة التنظيف
3. أوقف وضع التجربة عند الانتهاء من الاختبار
4. لا تستخدم وضع التجربة أبداً في بيئة الإنتاج

## ملاحظات مهمة
- وضع التجربة يحفظ حالته في localStorage
- يبقى مفعّلاً حتى بعد إعادة تحميل الصفحة
- يجب إيقافه يدوياً عند الانتهاء
- يظهر console.warn لكل عملية محظورة
