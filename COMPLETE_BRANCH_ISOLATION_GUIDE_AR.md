# دليل نظام عزل الفروع الكامل

## نظرة عامة

يصف هذا المستند التطبيق الكامل لنظام الفروع المتعددة مع **عزل صارم للبيانات** عبر جميع الوحدات في نظام Bloov للمحاسبة.

## هيكلية النظام

### المبادئ الأساسية

1. **عزل صارم للفروع**: كل فرع يعمل بشكل مستقل مع بياناته الخاصة
2. **وصول المدير العام**: المديرون العامون يمكنهم الوصول وإدارة جميع الفروع
3. **بحث عام عن العملاء**: جميع المستخدمين يمكنهم عرض جميع العملاء لمنع التكرار
4. **فرع المنشأ**: يتم تعيين العملاء للفرع الذي تم إنشاؤهم فيه أولاً
5. **كتالوج منتجات مشترك**: المنتجات مشتركة بين جميع الفروع
6. **مستويات مخزون معزولة**: كل فرع له كميات مخزون خاصة به

## قاعدة البيانات

### جداول جديدة

#### 1. `branches` (الفروع)
يخزن معلومات حول جميع الفروع في النظام.

```sql
- id (uuid, primary key)
- name (text) - اسم الفرع
- code (text, unique) - رمز الفرع (مثل "MAIN", "RIYADH01")
- location (text) - الموقع
- city (text) - المدينة
- phone (text) - الهاتف
- manager_id (uuid) - مدير الفرع
- is_active (boolean) - نشط؟
- opening_date (date) - تاريخ الافتتاح
- metadata (jsonb) - بيانات إضافية
- created_at, updated_at (timestamptz) - تواريخ الإنشاء والتحديث
```

#### 2. `branch_stock` (مخزون الفرع)
يتتبع كميات المخزون لكل فرع لكل منتج.

```sql
- id (uuid, primary key)
- branch_id (uuid) - معرف الفرع
- product_id (uuid) - معرف المنتج
- quantity (integer) - الكمية
- min_stock_level (integer) - الحد الأدنى للمخزون
- max_stock_level (integer) - الحد الأقصى للمخزون
- last_restock_date (timestamptz) - تاريخ آخر تعبئة
- created_at, updated_at (timestamptz)
- UNIQUE(branch_id, product_id)
```

#### 3. `setup_expenses` (مصاريف التأسيس)
يسجل مصاريف التأسيس/رأس المال لمرة واحدة للفروع.

### الجداول المعدلة

جميع الجداول التالية لديها الآن عمود `branch_id`:

- `users` - الفرع المخصص للمستخدم
- `sales` - الفرع الذي أجرى المبيعة
- `inventory` - المخزون مع تخصيص الفرع
- `expenses` - المصاريف التشغيلية لكل فرع
- `operating_expenses` - نفس المصاريف
- `customers` - فرع منشأ العميل
- `cash_transactions` - معاملات النقدية لكل فرع
- `cash_shifts` - نوبات الصندوق لكل فرع
- `purchases` - أوامر الشراء لكل فرع

## تحديثات المكونات

### 1. مكون المبيعات (`Sales.tsx`)

**التغييرات:**
- إضافة حالة `userBranchId` لتتبع فرع المستخدم
- إضافة `branch_id` عند إنشاء المبيعات
- تحديث جداول `branch_stock` و `inventory`
- تصفية استعلامات المخزون حسب `branch_id`
- جميع استعلامات UPDATE تتضمن `branch_id` في شرط WHERE

**الميزات الرئيسية:**
- المبيعات يتم تعيينها تلقائياً لفرع المستخدم
- يتم خصم المخزون من الفرع الصحيح
- المستخدمون يمكنهم رؤية مبيعات فرعهم فقط (إلا إذا كانوا مديرين عامين)

### 2. مكون المخزون (`Inventory.tsx`)

**التغييرات:**
- إضافة حالة `userBranchId`
- تسجيل التلف يتضمن تصفية `branch_id`
- الجرد اليدوي يتضمن تصفية `branch_id`
- جميع تحديثات المخزون لديها شروط WHERE صحيحة

**الميزات الرئيسية:**
- كل فرع يرى مخزونه فقط
- تعديلات المخزون تؤثر على مخزون الفرع فقط
- يمنع تعديلات المخزون عبر الفروع

### 3. مكون المصاريف (`Expenses.tsx`)

**التغييرات:**
- إضافة حالات `userBranchId` و `isSuperAdmin`
- تصفية المصاريف حسب الفرع
- إضافة `branch_id` عند إنشاء المصاريف

**الميزات الرئيسية:**
- المصاريف التشغيلية معزولة لكل فرع
- الإيجار، الكهرباء، الرواتب يتم تتبعها بشكل منفصل لكل فرع
- المديرون العامون يمكنهم عرض مصاريف جميع الفروع

### 4. مكون المشتريات (`Purchases.tsx`)

**التغييرات:**
- إضافة حالات `userBranchId` و `isSuperAdmin`
- تصفية المشتريات حسب الفرع
- إضافة `branch_id` عند إنشاء أوامر الشراء

**الميزات الرئيسية:**
- أوامر الشراء خاصة بكل فرع
- كل مدير فرع يرى طلباته فقط
- المديرون العامون يمكنهم عرض جميع المشتريات

### 5. مكون العملاء (`Customers.tsx`)

**التغييرات:**
- إضافة حقل `branch_id` إلى واجهة Customer
- إضافة علاقة `branches` لعرض فرع المنشأ
- تحميل جميع الفروع للرجوع إليها
- تعيين العملاء الجدد لفرع المستخدم
- بحث عام عن العملاء (جميع المستخدمين يمكنهم عرض جميع العملاء)

**الميزات الرئيسية:**
- **فرع المنشأ**: يتم تعيين العميل للفرع الذي تم إنشاؤه فيه أولاً
- **بحث عام**: جميع المستخدمين يمكنهم عرض جميع العملاء (يمنع التكرار)
- **إدارة مبنية على الفروع**: المستخدمون يمكنهم فقط تعديل/حذف عملاء من فرعهم
- يظهر الفرع الأصلي للعميل في الواجهة

## سياسات RLS (أمان مستوى الصف)

### نمط العزل الصارم

معظم الجداول تتبع هذا النمط:

```sql
-- SELECT: عرض فرعك فقط (أو الكل إذا كنت مدير عام)
CREATE POLICY "Users can view their branch data"
  ON table_name FOR SELECT
  TO authenticated
  USING (
    (SELECT role FROM users WHERE id = auth.uid()) = 'super_admin'
    OR branch_id = (SELECT branch_id FROM users WHERE id = auth.uid())
  );

-- INSERT: إنشاء لفرعك فقط
-- UPDATE: تعديل بيانات فرعك فقط
-- DELETE: حذف بيانات فرعك فقط
```

### حالات خاصة

#### جدول العملاء
- **SELECT**: جميع المستخدمين يمكنهم عرض جميع العملاء (بحث عام)
- **INSERT**: المستخدمون يمكنهم إنشاء عملاء (يتم تعيينهم لفرعهم)
- **UPDATE/DELETE**: المستخدمون يمكنهم فقط تعديل عملاء من فرعهم

#### جدول المنتجات
- **SELECT**: جميع المستخدمين يمكنهم عرض جميع المنتجات (كتالوج مشترك)
- **INSERT/UPDATE/DELETE**: صلاحيات الإدارة (غير خاصة بالفرع)

#### جدول مخزون الفرع
- معزول بشكل صارم لكل فرع
- المستخدمون يمكنهم فقط إدارة مخزون الفرع المخصص لهم

## الدوال المساعدة

### `is_super_admin()`
ترجع `true` إذا كان المستخدم الحالي لديه دور `super_admin`.

### `get_user_branch_id()`
ترجع `branch_id` للمستخدم المصادق عليه حالياً.

### `get_consolidated_sales_summary()`
**للمدير العام فقط**. ترجع ملخص المبيعات عبر جميع الفروع.

### `get_branch_stock_summary()`
ترجع ملخص المخزون لفرع محدد أو جميع الفروع (مدير عام).

## أدوار المستخدمين

### الأدوار العادية (وصول خاص بالفرع)
- **admin**: يمكنه إدارة المستخدمين والإعدادات داخل فرعه
- **manager**: يمكنه إدارة العمليات اليومية داخل فرعه
- **employee**: يمكنه إنشاء مبيعات وإدارة عملاء داخل فرعه
- **accountant**: يمكنه عرض البيانات المالية داخل فرعه
- **observer**: وصول للقراءة فقط داخل فرعه

### دور المدير العام
- **super_admin**: وصول كامل لجميع الفروع
  - يمكنه عرض وإدارة البيانات عبر جميع الفروع
  - يمكنه إنشاء وإدارة الفروع
  - يمكنه عرض التقارير الموحدة
  - يمكنه تعيين المستخدمين للفروع

## أمثلة على تدفق البيانات

### مثال 1: إنشاء مبيعة

1. مستخدم من الفرع أ ينشئ مبيعة
2. النظام يحمل `branch_id` للمستخدم (الفرع أ)
3. المبيعة تُنشأ مع `branch_id = الفرع أ`
4. المخزون يتم تحديثه للفرع أ فقط:
   - يخصم من `branch_stock` WHERE `branch_id = الفرع أ`
   - يخصم من `inventory` WHERE `branch_id = الفرع أ`
5. RLS يضمن أن المستخدم يمكنه الاستعلام عن بيانات الفرع أ فقط

### مثال 2: البحث عن عميل

1. مستخدم من الفرع ب يبحث عن عميل برقم الهاتف
2. النظام يظهر جميع العملاء (بحث عام)
3. إذا كان العميل موجود من الفرع أ، المستخدم يمكنه:
   - **العرض**: نعم (بحث عام)
   - **التعديل**: لا (العميل ينتمي للفرع أ)
   - **إنشاء مبيعة**: نعم (يمكنه البيع لأي عميل)
4. المبيعة تُنشأ مع:
   - `customer_id` = العميل الموجود
   - `branch_id` = الفرع ب (منشأ المبيعة)

### مثال 3: تقارير المدير العام

1. المدير العام يسجل دخول
2. يمكنه اختيار "جميع الفروع" أو فرع محدد
3. يرى البيانات الموحدة:
   - إجمالي المبيعات عبر جميع الفروع
   - مستويات المخزون في جميع المواقع
   - جميع المصاريف من جميع الفروع
4. يمكنه التعمق في تفاصيل فرع محدد

## ملاحظات الترحيل

### البيانات الموجودة

تم تعيين جميع البيانات الموجودة إلى "الفرع الرئيسي" (الرمز: MAIN) أثناء الترحيل.

### إضافة فروع جديدة

لإضافة فرع جديد:

1. **إنشاء سجل الفرع**:
```sql
INSERT INTO branches (name, code, location, city, phone, is_active)
VALUES ('فرع الرياض 2', 'RYD02', 'حي العليا', 'الرياض', '+966501234567', true);
```

2. **تعيين المدير**:
```sql
UPDATE branches SET manager_id = 'user-uuid' WHERE code = 'RYD02';
```

3. **تعيين المستخدمين للفرع**:
```sql
UPDATE users SET branch_id = (SELECT id FROM branches WHERE code = 'RYD02')
WHERE id IN ('user1-uuid', 'user2-uuid');
```

4. **تهيئة مخزون الفرع**:
```sql
-- نسخ المنتجات إلى مخزون الفرع الجديد
INSERT INTO branch_stock (branch_id, product_id, quantity, min_stock_level)
SELECT
  (SELECT id FROM branches WHERE code = 'RYD02'),
  id,
  0, -- الكمية الابتدائية
  min_stock_level
FROM products
WHERE is_active = true;
```

## قائمة الاختبار

### اختبارات عزل الفروع

- [ ] مستخدم من الفرع أ لا يمكنه رؤية مبيعات الفرع ب
- [ ] مستخدم من الفرع أ لا يمكنه رؤية مصاريف الفرع ب
- [ ] مستخدم من الفرع أ لا يمكنه رؤية مشتريات الفرع ب
- [ ] مستخدم من الفرع أ لا يمكنه رؤية مخزون الفرع ب
- [ ] مستخدم من الفرع أ لا يمكنه تعديل بيانات في الفرع ب
- [ ] المدير العام يمكنه رؤية وتعديل بيانات جميع الفروع

### اختبارات العملاء

- [ ] مستخدم من الفرع أ يمكنه عرض عملاء من الفرع ب (بحث عام)
- [ ] مستخدم من الفرع أ لا يمكنه تعديل عملاء من الفرع ب
- [ ] مستخدم من الفرع أ يمكنه إنشاء مبيعة لعميل من الفرع ب
- [ ] العميل الجديد يتم تعيينه لفرع المستخدم المنشئ
- [ ] البحث عن العميل يظهر جميع العملاء في النظام

### اختبارات إدارة المخزون

- [ ] مبيعة في الفرع أ تخصم مخزون من الفرع أ فقط
- [ ] مبيعة في الفرع ب تخصم مخزون من الفرع ب فقط
- [ ] تلف المخزون في الفرع أ يؤثر على الفرع أ فقط
- [ ] الجرد اليدوي في الفرع أ يؤثر على الفرع أ فقط
- [ ] كل فرع لديه مستويات مخزون مستقلة لنفس المنتج

### اختبارات المدير العام

- [ ] المدير العام يمكنه عرض جميع الفروع
- [ ] المدير العام يمكنه التبديل بين الفروع
- [ ] المدير العام يمكنه إنشاء/تعديل/حذف في أي فرع
- [ ] المدير العام يمكنه عرض التقارير الموحدة
- [ ] المدير العام يمكنه إدارة إعدادات الفروع

## اعتبارات الأمان

1. **RLS إلزامي**: جميع الجداول مع `branch_id` لديها RLS مفعل
2. **لا يمكن التجاوز**: كود التطبيق لا يمكنه تجاوز سياسات RLS
3. **أمان الدوال**: الدوال المساعدة تستخدم `SECURITY DEFINER` بحذر
4. **التحقق من الدور**: دائماً تحقق من دور المستخدم في السياسات
5. **معالجة Null**: السياسات تتعامل مع `branch_id IS NULL` للبيانات القديمة

## تحسين الأداء

1. **الفهارس**: جميع أعمدة `branch_id` مفهرسة
2. **تحسين الاستعلامات**: الواجهة الأمامية تصفي حسب الفرع حيثما أمكن
3. **تخزين RLS مؤقت**: الدوال المساعدة معلمة بـ `STABLE` للتخزين المؤقت
4. **التحميل الانتقائي**: تحميل البيانات الضرورية فقط للفرع الحالي

## استكشاف الأخطاء وإصلاحها

### المشكلة: "UPDATE requires a WHERE clause"
**السبب**: `branch_id` مفقود في شرط WHERE للتحديث
**الحل**: دائماً قم بتضمين `.eq('branch_id', userBranchId)` في التحديثات

### المشكلة: المستخدم لا يرى أي بيانات بعد تسجيل الدخول
**السبب**: المستخدم غير مخصص لفرع
**الحل**: قم بتخصيص المستخدم لفرع عبر لوحة الإدارة

### المشكلة: خصم المخزون من فرع خاطئ
**السبب**: عدم تحميل branch_id للمستخدم قبل العمليات
**الحل**: استدعِ `loadUserBranch()` في useEffect للمكون

### المشكلة: لا يمكن تعديل العميل
**السبب**: العميل ينتمي لفرع مختلف
**الحل**: فقط المستخدمون من فرع منشأ العميل يمكنهم التعديل

## التحسينات المستقبلية

1. **نقل الفرع**: السماح بنقل المخزون بين الفروع
2. **التقارير الموحدة**: تقارير محسنة للمديرين العامين
3. **صلاحيات الفرع**: صلاحيات دقيقة لكل فرع
4. **تحليلات الفرع**: مقارنة الأداء عبر الفروع
5. **طلبات بين الفروع**: وضع طلبات بين الفروع

## الملفات ذات الصلة

### مكونات الواجهة الأمامية
- `src/components/Sales.tsx`
- `src/components/Inventory.tsx`
- `src/components/Expenses.tsx`
- `src/components/Purchases.tsx`
- `src/components/Customers.tsx`
- `src/components/Branches.tsx`

### ترحيلات قاعدة البيانات
- `20260214011240_create_multi_branch_system_v3.sql`
- `20260214011306_assign_existing_data_to_default_branch.sql`
- `20260214120000_update_customer_rls_for_branch_of_origin.sql`
- `20260214120001_update_inventory_rls_for_strict_branch_isolation.sql`
- `20260214120002_update_operating_expenses_rls_for_branch_isolation.sql`
- `20260214120003_update_cash_transactions_rls_for_branch_isolation.sql`
- `20260214120004_update_cash_shifts_rls_for_branch_isolation.sql`

## الخلاصة

نظام عزل الفروع الكامل يضمن أن كل فرع يعمل بشكل مستقل مع الحفاظ على سلامة البيانات ومنع الوصول غير المصرح به عبر الفروع. المديرون العامون لديهم رؤية كاملة وتحكم عبر جميع الفروع، بينما المستخدمون العاديون مقيدون بالفرع المخصص لهم.

---

**التاريخ**: 2026-02-14
**الإصدار**: 1.0
**الحالة**: جاهز للإنتاج ✅
