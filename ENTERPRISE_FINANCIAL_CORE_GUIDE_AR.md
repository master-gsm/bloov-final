# نواة النظام المالي المؤسسي - دليل التنفيذ
## نظام دفتر الأستاذ العام متعدد العملات

**التاريخ:** 16 فبراير 2026
**الحالة:** ✅ تم النشر بنجاح
**Migration:** `recreate_enterprise_financial_core.sql`

---

## 📊 الملخص التنفيذي

نظام محاسبة مالية متكامل على مستوى المؤسسات مع دعم العملات المتعددة، قفل الفترات، والقيود غير القابلة للتعديل. بُني على 5 أعمدة أساسية للنزاهة المالية.

**المميزات الرئيسية:**
- ✅ معاملات متعددة العملات مع تحويل تلقائي للعملة الأساسية
- ✅ إدارة الفترات المحاسبية مع حالة مفتوح/مغلق
- ✅ قيود مرحّلة غير قابلة للتعديل (لا يمكن تعديلها أو حذفها)
- ✅ نمط القيود العكسية للتصحيحات
- ✅ فرض توازن القيد المزدوج الصارم
- ✅ عزل على مستوى الفرع للقيود المحاسبية
- ✅ دليل حسابات عام (مشترك بين الفروع)

---

## 🏛️ معمارية الأعمدة الخمسة

### العمود الأول: الهيكل (الجداول الأساسية)

#### 1.1 الفترات المحاسبية
**الغرض:** إدارة الإغلاق المالي وقفل الفترات

```sql
accounting_periods (
  id UUID PRIMARY KEY,
  name VARCHAR(100) UNIQUE,
  start_date DATE,
  end_date DATE,
  is_closed BOOLEAN DEFAULT false,
  closed_at TIMESTAMPTZ,
  closed_by UUID REFERENCES users
)
```

**المميزات:**
- التحقق من نطاق التاريخ (end_date >= start_date)
- تتبع حالة الإغلاق مع سجل المراجعة
- منع الترحيل إلى الفترات المغلقة

**بيانات مثال:**
- يناير 2026: من 2026-01-01 إلى 2026-01-31 (مفتوح)
- فبراير 2026: من 2026-02-01 إلى 2026-02-28 (مفتوح)

---

#### 1.2 دليل الحسابات (عام)
**الغرض:** هيكل حسابات موحد عبر جميع الفروع

```sql
accounts (
  id UUID PRIMARY KEY,
  code VARCHAR(20) UNIQUE,
  name VARCHAR(200),
  name_ar VARCHAR(200),
  type VARCHAR(20), -- Asset/Liability/Equity/Revenue/Expense/COGS
  parent_id UUID REFERENCES accounts,
  is_active BOOLEAN DEFAULT true,
  is_system BOOLEAN DEFAULT false
)
```

**المميزات:**
- هيكل هرمي (علاقات أب-ابن)
- حماية الحسابات النظامية (لا يمكن حذفها)
- دعم متعدد اللغات (EN/AR)
- نطاق عام (لا branch_id)

**أنواع الحسابات:**
- الأصول (1000-1999)
- الخصوم (2000-2999)
- حقوق الملكية (3000-3999)
- الإيرادات (4000-4999)
- تكلفة البضاعة المباعة (5000-5999)
- المصروفات (6000-6999)

**الدليل المعياري يتضمن 33 حساب:**
- 15 حساب أصول (نقدية، مدينون، مخزون، أصول ثابتة)
- 7 حسابات خصوم (دائنون، قروض، ضريبة)
- 4 حسابات حقوق ملكية (رأس المال، أرباح محتجزة)
- 3 حسابات إيرادات (مبيعات، خدمات)
- 2 حسابات تكلفة
- 2 حسابات مصروفات (رواتب، إيجار، مرافق)

---

#### 1.3 القيود المحاسبية (الرؤوس)
**الغرض:** بيانات القيد الوصفية مع دعم العملات المتعددة

```sql
journal_entries (
  id UUID PRIMARY KEY,
  entry_number VARCHAR(50) UNIQUE, -- JE-YYYY-NNNN (غير قابل للتغيير)
  date DATE,
  description TEXT,
  status VARCHAR(20), -- Draft/Posted/Void
  branch_id UUID REFERENCES branches,

  -- العملات المتعددة
  currency_code VARCHAR(3) DEFAULT 'SAR',
  exchange_rate NUMERIC(15, 6) DEFAULT 1.0,

  -- التحكم في الفترة
  period_locked BOOLEAN DEFAULT false,

  -- تتبع العكس
  original_entry_id UUID REFERENCES journal_entries,
  reverse_entry_id UUID REFERENCES journal_entries,

  -- سجل المراجعة
  created_by UUID,
  posted_by UUID,
  voided_by UUID,
  created_at TIMESTAMPTZ,
  posted_at TIMESTAMPTZ,
  voided_at TIMESTAMPTZ
)
```

**المميزات:**
- أرقام قيود مُولدة تلقائياً (JE-2026-0001, JE-2026-0002, ...)
- عملات متعددة مع تتبع سعر الصرف
- دورة حالة: مسودة → مرحّل → ملغى
- تكامل قفل الفترة
- سجل مراجعة كامل مع الطوابع الزمنية والمستخدمين
- ربط القيود العكسية

**دورة الحالة:**
```
مسودة → مرحّل (عندما يكون متوازن وصحيح)
مرحّل → ملغى (عبر دالة void_journal_entry فقط)
```

---

#### 1.4 أسطر القيود (التفاصيل)
**الغرض:** أسطر المدين/الدائن الفردية مع تحويل تلقائي للعملة

```sql
journal_lines (
  id UUID PRIMARY KEY,
  journal_entry_id UUID REFERENCES journal_entries ON DELETE RESTRICT,
  account_id UUID REFERENCES accounts,

  -- عملة المعاملة
  debit NUMERIC(15, 2) DEFAULT 0,
  credit NUMERIC(15, 2) DEFAULT 0,

  -- العملة الأساسية (SAR) - محسوبة تلقائياً
  base_debit NUMERIC(15, 2) DEFAULT 0,
  base_credit NUMERIC(15, 2) DEFAULT 0,

  description TEXT,
  line_number INTEGER
)
```

**المميزات:**
- مبالغ عملة المعاملة (مدين/دائن)
- مبالغ العملة الأساسية محسوبة تلقائياً (ريال)
- قيود من جانب واحد فقط (إما مدين أو دائن، وليس كلاهما)
- DELETE RESTRICT (لا يمكن حذف الأسطر إذا كان القيد موجوداً)
- ترقيم الأسطر للترتيب

**قواعد التحقق:**
- المبالغ يجب أن تكون >= 0
- إما مدين أو دائن (وليس كلاهما)
- يجب أن يكون مبلغ واحد على الأقل > 0
- المبالغ الأساسية محسوبة تلقائياً عند الإدخال/التحديث

---

### العمود الثاني: المنطق المالي (Triggers)

#### 2.1 محرك العملات المتعددة
**Trigger:** `trg_calculate_base_currency`
**Function:** `calculate_base_currency_amounts()`

**الغرض:** تحويل تلقائي من عملة المعاملة إلى العملة الأساسية (ريال)

**المنطق:**
```sql
base_debit = debit × exchange_rate
base_credit = credit × exchange_rate
```

**مثال:**
```
المعاملة: 100 دولار أمريكي مدين
سعر الصرف: 3.75
النتيجة: base_debit = 375.00 ريال
```

**الأمان:** SECURITY DEFINER مع تثبيت search_path

---

#### 2.2 فرض قفل الفترة
**Trigger:** `trg_check_period_lock`
**Function:** `check_period_lock()`

**الغرض:** منع الترحيل إلى الفترات المحاسبية المغلقة

**المنطق:**
1. التحقق إذا كان تاريخ القيد يقع ضمن فترة مغلقة
2. إذا نعم، رفع استثناء: "لا يمكن الترحيل إلى فترة مغلقة: [الاسم]"
3. إذا في فترة مفتوحة، وضع علامة على القيد بـ period_locked = false
4. يتحقق فقط عند INSERT أو UPDATE عندما status = 'Posted'

**مثال:**
```
تاريخ القيد: 2026-01-15
الفترة: يناير 2026 (is_closed = true)
النتيجة: خطأ - لا يمكن الترحيل إلى فترة مغلقة
```

**الأمان:** SECURITY DEFINER مع تثبيت search_path

---

#### 2.3 فرض التوازن الصارم
**Trigger:** `trg_enforce_strict_balance`
**Function:** `enforce_strict_balance()`

**الغرض:** فرض توازن محاسبة القيد المزدوج قبل الترحيل

**فحوصات التحقق:**
1. **الحد الأدنى للأسطر:** يجب أن يحتوي القيد على سطرين على الأقل
2. **التوازن:** SUM(base_debit) يجب أن يساوي SUM(base_credit) (التسامح: 0.01)
3. **متطلبات الإلغاء:** حالة الإلغاء تتطلب reverse_entry_id

**مثال:**
```
السطر 1: مدين 1000 ريال (نقدية)
السطر 2: دائن 1000 ريال (إيرادات)
الإجمالي: 1000 = 1000 → ✅ متوازن

السطر 1: مدين 1000 ريال
السطر 2: دائن 900 ريال
الإجمالي: 1000 ≠ 900 → ❌ خطأ
```

**الأمان:** SECURITY DEFINER مع تثبيت search_path

---

#### 2.4 حماية عدم القابلية للتغيير (الرؤوس)
**Trigger:** `trg_protect_posted_entries`
**Function:** `protect_posted_entries()`

**الغرض:** منع تعديل القيود المرحّلة/الملغاة

**القواعد:**
- ❌ لا يمكن حذف القيود المرحّلة أو الملغاة
- ❌ لا يمكن تحديث القيود المرحّلة (باستثناء تغيير الحالة إلى ملغى)
- ❌ لا يمكن تحديث القيود الملغاة على الإطلاق
- ❌ لا يمكن تعديل entry_number (غير قابل للتغيير دائماً)

**العمليات المسموحة:**
- ✅ تحديث الحالة من مرحّل → ملغى (عبر دالة void)
- ✅ حذف قيود المسودة

**الأمان:** SECURITY DEFINER مع تثبيت search_path

---

#### 2.5 حماية عدم القابلية للتغيير (الأسطر)
**Trigger:** `trg_protect_posted_lines`
**Function:** `protect_posted_entry_lines()`

**الغرض:** منع تعديل الأسطر في القيود المرحّلة/الملغاة

**القواعد:**
- ❌ لا يمكن إدراج أسطر جديدة في القيود المرحّلة/الملغاة
- ❌ لا يمكن تحديث الأسطر الموجودة في القيود المرحّلة/الملغاة
- ❌ لا يمكن حذف أسطر من القيود المرحّلة/الملغاة

**الأمان:** SECURITY DEFINER مع تثبيت search_path

---

#### 2.6 توليد رقم القيد
**Trigger:** `trg_generate_entry_number`
**Function:** `generate_entry_number()`

**الغرض:** توليد تلقائي لأرقام القيود المتسلسلة

**الصيغة:** `JE-YYYY-NNNN`
- JE = قيد محاسبي (Journal Entry)
- YYYY = السنة من تاريخ القيد
- NNNN = رقم متسلسل (محشو إلى 4 أرقام)

**أمثلة:**
- JE-2026-0001
- JE-2026-0002
- JE-2027-0001 (إعادة التعيين كل عام)

**الأمان:** SECURITY DEFINER مع تثبيت search_path

---

### العمود الثالث: منطق الإلغاء

#### Function: void_journal_entry(p_entry_id UUID, p_reason TEXT)
**يُرجع:** UUID (معرف القيد العكسي)

**الغرض:** إنشاء قيد عكسي لتصحيح المعاملات المرحّلة

**العملية:**
1. **التحقق:** يجب أن يكون القيد مرحّلاً (وليس مسودة أو ملغى بالفعل)
2. **التحقق من الفترة:** لا يمكن إلغاء القيود في الفترات المغلقة
3. **إنشاء قيد عكسي:** قيد جديد بمدين/دائن معكوس
4. **ربط القيود:** تعيين original_entry_id و reverse_entry_id
5. **وضع علامة ملغى:** تحديث حالة القيد الأصلي إلى 'Void'

**مثال:**
```sql
-- القيد الأصلي (JE-2026-0001)
السطر 1: مدين 1000 (نقدية)
السطر 2: دائن 1000 (إيرادات)

-- بعد void_journal_entry('...', 'مبلغ خاطئ')

-- القيد العكسي (JE-2026-0002)
السطر 1: مدين 1000 (إيرادات) -- معكوس
السطر 2: دائن 1000 (نقدية)   -- معكوس

-- حالة القيد الأصلي: ملغى
-- الربط: أصلي ↔ عكسي
```

**الأمان:** SECURITY DEFINER مع تثبيت search_path

---

### العمود الرابع: البيانات الأولية

#### دليل الحسابات (33 حساب)

**الأصول (15 حساب):**
```
1000  الأصول
├── 1100  الأصول المتداولة
│   ├── 1110  النقدية وما يعادلها
│   │   ├── 1111  النقدية في الصندوق
│   │   └── 1112  حسابات بنكية
│   ├── 1120  المدينون
│   │   └── 1121  ذمم العملاء
│   └── 1130  المخزون
│       ├── 1131  مواد خام
│       └── 1132  بضاعة جاهزة
└── 1200  الأصول غير المتداولة
    └── 1210  الممتلكات والمعدات
        ├── 1211  أراضي
        ├── 1212  مباني
        └── 1213  معدات
```

**الخصوم (7 حسابات):**
```
2000  الخصوم
├── 2100  الخصوم المتداولة
│   ├── 2110  الدائنون
│   │   └── 2111  ذمم الموردين
│   └── 2130  ضريبة القيمة المضافة
```

**حقوق الملكية (4 حسابات):**
```
3000  حقوق الملكية
├── 3100  رأس المال
├── 3200  الأرباح المحتجزة
└── 3300  ربح/خسارة السنة الحالية
```

**الإيرادات (3 حسابات):**
```
4000  الإيرادات
└── 4100  إيرادات المبيعات
    └── 4110  مبيعات منتجات
```

**تكلفة البضاعة المباعة (2 حساب):**
```
5000  تكلفة البضاعة المباعة
└── 5100  التكاليف المباشرة
```

**المصروفات (2 حساب):**
```
6000  المصروفات التشغيلية
├── 6100  الرواتب والأجور
├── 6200  إيجارات
└── 6300  المرافق
```

#### الفترات المحاسبية (فترتان)
- يناير 2026: من 2026-01-01 إلى 2026-01-31 (مفتوح)
- فبراير 2026: من 2026-02-01 إلى 2026-02-28 (مفتوح)

---

### العمود الخامس: الأمان (سياسات RLS)

#### الفترات المحاسبية
- ✅ جميع المستخدمين يمكنهم عرض الفترات
- ✅ المشرفون/المشرف العام يمكنهم إدارة الفترات

#### الحسابات (عام)
- ✅ جميع المستخدمين يمكنهم عرض الحسابات
- ✅ المشرفون/المشرف العام يمكنهم إدارة الحسابات

#### القيود المحاسبية (عزل الفرع)
- ✅ المستخدمون يمكنهم عرض القيود في فرعهم
- ✅ المشرف العام يمكنه عرض جميع القيود
- ✅ المستخدمون يمكنهم إنشاء قيود في فرعهم
- ✅ المستخدمون يمكنهم تحديث قيود في فرعهم
- ✅ المستخدمون يمكنهم حذف قيود المسودة فقط

#### أسطر القيود
- ✅ المستخدمون يمكنهم عرض أسطر القيود المتاحة
- ✅ المستخدمون يمكنهم إدارة أسطر قيودهم

---

## 💼 أمثلة الاستخدام

### مثال 1: بيع نقدي بسيط (ريال)

```sql
-- الخطوة 1: إنشاء رأس القيد
INSERT INTO journal_entries (
  date, description, branch_id, currency_code, exchange_rate, created_by
) VALUES (
  '2026-02-16',
  'بيع نقدي - فاتورة #1001',
  '...branch_id...',
  'SAR',
  1.0,
  '...user_id...'
) RETURNING id;  -- يُرجع: abc123...

-- الخطوة 2: إضافة الأسطر
INSERT INTO journal_lines (journal_entry_id, account_id, debit, line_number) VALUES
('abc123...', (SELECT id FROM accounts WHERE code = '1111'), 1000.00, 1);  -- نقدية

INSERT INTO journal_lines (journal_entry_id, account_id, credit, line_number) VALUES
('abc123...', (SELECT id FROM accounts WHERE code = '4110'), 1000.00, 2);  -- مبيعات

-- الخطوة 3: ترحيل القيد
UPDATE journal_entries SET status = 'Posted' WHERE id = 'abc123...';
-- النظام يتحقق: سطران ✓، متوازن ✓، الفترة مفتوحة ✓
-- النتيجة: تم ترحيل القيد بنجاح
```

**النتيجة:**
```
القيد: JE-2026-0001
الحالة: مرحّل
التاريخ: 2026-02-16

السطر 1: 1111 النقدية في الصندوق    1000.00 مدين
السطر 2: 4110 مبيعات منتجات                      1000.00 دائن
         الإجمالي                   1000.00    1000.00
```

---

### مثال 2: شراء متعدد العملات (دولار)

```sql
-- الخطوة 1: إنشاء قيد (دولار مع سعر صرف)
INSERT INTO journal_entries (
  date, description, branch_id, currency_code, exchange_rate, created_by
) VALUES (
  '2026-02-16',
  'شراء معدات من أمريكا',
  '...branch_id...',
  'USD',
  3.75,  -- 1 دولار = 3.75 ريال
  '...user_id...'
) RETURNING id;  -- يُرجع: def456...

-- الخطوة 2: إضافة الأسطر (بالدولار)
INSERT INTO journal_lines (journal_entry_id, account_id, debit, line_number) VALUES
('def456...', (SELECT id FROM accounts WHERE code = '1213'), 5000.00, 1);  -- معدات (دولار)

INSERT INTO journal_lines (journal_entry_id, account_id, credit, line_number) VALUES
('def456...', (SELECT id FROM accounts WHERE code = '1112'), 5000.00, 2);  -- بنك (دولار)

-- الخطوة 3: ترحيل القيد
UPDATE journal_entries SET status = 'Posted' WHERE id = 'def456...';
```

**النتيجة:**
```
القيد: JE-2026-0002
الحالة: مرحّل
العملة: USD (السعر: 3.75)

السطر 1: 1213 معدات           5000.00 مدين (18750.00 ريال)
السطر 2: 1112 حسابات بنكية              5000.00 دائن (18750.00 ريال)
         الإجمالي (دولار)     5000.00    5000.00
         الإجمالي (ريال)    18750.00   18750.00
```

**التحويل للعملة الأساسية:**
- معدات: 5000 × 3.75 = 18,750 ريال
- بنك: 5000 × 3.75 = 18,750 ريال
- التوازن: 18,750 = 18,750 ✅

---

### مثال 3: إلغاء قيد

```sql
-- إلغاء البيع النقدي من المثال 1
SELECT void_journal_entry(
  'abc123...',  -- معرف القيد
  'العميل أرجع البضاعة - استرداد كامل'
);

-- يُرجع: xyz789... (معرف القيد العكسي)
```

**النتيجة:**
```
القيد الأصلي: JE-2026-0001
الحالة: ملغى (voided_at: 2026-02-16, voided_by: مستخدم)
reverse_entry_id: xyz789...

القيد العكسي: JE-2026-0003
الحالة: مرحّل
الوصف: عكس: بيع نقدي - فاتورة #1001 | السبب: العميل أرجع البضاعة

السطر 1: 4110 مبيعات منتجات  1000.00 مدين  (معكوس)
السطر 2: 1111 النقدية في الصندوق            1000.00 دائن  (معكوس)
```

**التأثير الصافي:**
- الأصلي: +1000 نقدية، +1000 إيرادات
- العكسي: -1000 نقدية، -1000 إيرادات
- الصافي: 0 (المعاملة معكوسة بالكامل)

---

## ✅ قائمة التحقق

### مكونات النظام
- ✅ 4 جداول تم إنشاؤها (accounting_periods, accounts, journal_entries, journal_lines)
- ✅ 7 دوال تم نشرها (جميعها مع SECURITY DEFINER + تثبيت search_path)
- ✅ 6 Triggers تم تفعيلها (عملات متعددة، قفل الفترة، التوازن، عدم القابلية للتغيير)
- ✅ 33 حساب تم إضافتها (دليل حسابات كامل)
- ✅ فترتان تم إنشاؤها (يناير-فبراير 2026)
- ✅ سياسات RLS مفعّلة (عزل الفرع)

### القواعد المالية
- ✅ دعم العملات المتعددة مع تحويل تلقائي
- ✅ فرض قفل الفترة
- ✅ التحقق من توازن القيد المزدوج
- ✅ عدم قابلية القيود المرحّلة للتغيير
- ✅ نمط التصحيح عبر الإلغاء فقط
- ✅ ترقيم متسلسل للقيود
- ✅ سجل مراجعة كامل

### الأمان
- ✅ جميع دوال SECURITY DEFINER لديها search_path مثبت
- ✅ RLS مفعّل على جميع الجداول
- ✅ عزل الفرع للقيود
- ✅ حسابات عامة (لا عزل)
- ✅ إدارة الفترة للمشرفين فقط

---

## 🎉 الملخص

**تم نشر نواة النظام المالي المؤسسي بنجاح مع:**

✅ **دفتر أستاذ عام متعدد العملات** - دعم أي عملة مع تحويل تلقائي للريال
✅ **قفل الفترات** - التحكم في الإغلاق المالي ونهائية التقارير
✅ **محاسبة غير قابلة للتغيير** - لا يمكن تعديل أو حذف القيود المرحّلة
✅ **نمط القيود العكسية** - التصحيحات عبر دالة void (يُحفظ سجل المراجعة)
✅ **قيد مزدوج صارم** - فرض التوازن عند الترحيل
✅ **عزل الفرع** - دعم متعدد الفروع مع فصل البيانات المناسب
✅ **دليل حسابات عام** - هيكل حسابات موحد عبر المؤسسة
✅ **تقوية أمنية** - جميع الدوال محمية ضد Schema Hijacking

**الحالة:** ✅ جاهز للإنتاج
**Migration:** `recreate_enterprise_financial_core.sql`
**تاريخ النشر:** 16 فبراير 2026
