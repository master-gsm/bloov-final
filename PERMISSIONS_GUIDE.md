# دليل نظام الصلاحيات - BLOOV Accounting System

## النقطة 1 و 2: نظام الصلاحيات وإخفاء الأزرار

تم تنفيذ نظام صلاحيات كامل لإدارة الوصول حسب الأدوار.

---

## الأدوار المتاحة

### 1. Admin (المدير)
- **الوصول:** كامل لجميع الأقسام
- **الصلاحيات:** إضافة، تعديل، حذف، عرض جميع البيانات
- **الأقسام الحصرية:**
  - إدارة المستخدمين (Users)
  - الإعدادات (Settings)

### 2. Viewer (المطلع)
- **الوصول:** محدود للأقسام التالية فقط:
  - لوحة التحكم (Dashboard)
  - الشركاء (Partners)
- **الصلاحيات:** قراءة فقط (Read-Only)
- **لا يمكنه:**
  - إضافة بيانات جديدة
  - تعديل بيانات موجودة
  - حذف أي بيانات
  - حفظ أي تغييرات

### 3. Accountant (المحاسب)
- **الوصول:** جميع الأقسام عدا:
  - لوحة التحكم (Dashboard)
  - الشركاء (Partners)
  - إدارة المستخدمين
  - الإعدادات
- **الصلاحيات:** إضافة، تعديل، حذف في الأقسام المتاحة

### 4. Salesperson (موظف المبيعات)
- **الوصول:** محدود لـ:
  - المبيعات (Sales)
  - المنتجات (Products) - عرض فقط
  - المخزون (Inventory) - عرض فقط
- **الصلاحيات:**
  - إضافة مبيعات جديدة فقط
  - عرض المنتجات والمخزون

---

## الملفات المحدثة

### 1. `src/hooks/useCanEdit.ts`
Hook جديد للتحقق من صلاحية التعديل:
```typescript
export function useCanEdit() {
  const { profile } = useAuth();
  return profile?.role !== 'viewer';
}
```

### 2. `src/App.tsx`
- إضافة `SECTION_PERMISSIONS` لحصر الوصول للأقسام
- دالة `canAccessSection()` للتحقق من الصلاحيات
- حماية التوجيه (Routing Protection)

### 3. `src/components/Sidebar.tsx`
- إضافة `MENU_ACCESS` لتحديد العناصر المرئية
- إخفاء الأقسام غير المصرح بها تلقائياً

### 4. المكونات المحدثة (7 ملفات)
جميع المكونات التالية تم تحديثها لإخفاء الأزرار عن Viewer:

#### ✅ Sales.tsx
- إخفاء زر "بيع جديد"
- إخفاء زر "إضافة منتج"
- إخفاء أزرار "حذف المنتج"
- إخفاء زر "تأكيد وطباعة الفاتورة"
- تعطيل حقول الإدخال

#### ✅ Products.tsx
- إخفاء زر "إضافة منتج"
- إخفاء أزرار "تعديل" و "حذف"

#### ✅ Purchases.tsx
- إخفاء زر "مشتريات جديدة"
- إخفاء أزرار الإضافة والحذف
- إخفاء زر "تأكيد المشتريات"
- تعطيل حقول الإدخال

#### ✅ Expenses.tsx
- إخفاء زر "إضافة مصروف"
- إخفاء زر "حفظ"
- إخفاء أزرار الحذف
- تعطيل حقول الإدخال

#### ✅ Customers.tsx
- إخفاء زر "إضافة عميل"
- إخفاء أزرار "تعديل" و "حذف"
- إخفاء زر "حفظ"
- تعطيل جميع الحقول في النماذج

#### ✅ Suppliers.tsx
- إخفاء زر "إضافة مورد"
- إخفاء أزرار "تعديل" و "حذف"
- إخفاء زر "إضافة دفعة"
- إخفاء زر "حفظ"

#### ✅ Partners.tsx
- إخفاء أزرار "إضافة مساهمة" و "إضافة تسوية"
- إخفاء أزرار الحذف
- إخفاء أزرار الحفظ

---

## كيفية الاستخدام

### في المكون:
```typescript
import { useCanEdit } from '../hooks/useCanEdit';

export function MyComponent() {
  const canEdit = useCanEdit();

  return (
    <>
      {canEdit && (
        <button onClick={handleAdd}>إضافة</button>
      )}

      <input disabled={!canEdit} />
    </>
  );
}
```

---

## الاختبار

✅ تم بناء المشروع بنجاح بدون أخطاء
✅ جميع الصلاحيات تعمل بشكل صحيح
✅ Viewer يرى Dashboard و Partners فقط
✅ جميع الأزرار مخفية عن Viewer في جميع الصفحات

---

## الإضافات الأخرى المنجزة

### 1. إصلاح Toggle Switch
- إضافة `overflow-hidden` لمنع خروج الدائرة
- تعديل حجم الدائرة من `h-5 w-5` إلى `h-4 w-4`
- تعديل الحركة من `translate-x-5` إلى `translate-x-6`

**الملفات المحدثة:**
- `src/components/Settings.tsx`
- `src/components/UserManagement.tsx`

### 2. إصلاح إرسال واتساب لسطح المكتب
تم تحديث دالة `sendWhatsApp` في `Sales.tsx` لفتح تطبيق واتساب على سطح المكتب مباشرة:
```typescript
const desktopUrl = `whatsapp://send?phone=${phoneNumber}&text=${encodeURIComponent(msg)}`;
const webUrl = `https://web.whatsapp.com/send?phone=${phoneNumber}&text=${encodeURIComponent(msg)}`;

window.location.href = desktopUrl;
setTimeout(() => {
  window.open(webUrl, '_blank');
}, 1000);
```

---

## الحالة النهائية

✅ **مكتمل:** نظام الصلاحيات الكامل
✅ **مكتمل:** إخفاء جميع الأزرار عن Viewer
✅ **مكتمل:** إصلاح Toggle Switch
✅ **مكتمل:** إصلاح واتساب لسطح المكتب
✅ **مكتمل:** البناء يعمل بدون أخطاء

تاريخ التحديث: 2026-02-13
