# دليل تطبيق سطح المكتب - Bloov Accounting

تم تحويل نظام Bloov للمحاسبة إلى تطبيق سطح مكتب يعمل على Windows, Mac, و Linux.

---

## المتطلبات

- Node.js (النسخة 16 أو أحدث)
- npm أو yarn

---

## التطوير المحلي

### 1. تثبيت المكتبات
```bash
npm install
```

### 2. تشغيل التطبيق في وضع التطوير
```bash
npm run electron:dev
```

هذا الأمر سيفتح نافذة Electron مع التطبيق وإعادة التحميل التلقائي.

---

## بناء ملف التنصيب (exe)

### لنظام Windows (64-bit)
```bash
npm run electron:build:win
```

**النتيجة:**
- ملف التنصيب: `release/Bloov-Accounting-Setup-1.0.0.exe`
- يمكنك توزيع هذا الملف لأي جهاز Windows

### لنظام Mac
```bash
npm run electron:build:mac
```

### لنظام Linux
```bash
npm run electron:build:linux
```

### بناء لجميع الأنظمة
```bash
npm run electron:build
```

---

## الملفات الناتجة

بعد البناء، ستجد الملفات في مجلد `release/`:

```
release/
  ├── Bloov-Accounting-Setup-1.0.0.exe  (Windows Installer)
  ├── win-unpacked/                      (Windows مجلد محمول)
  └── builder-effective-config.yaml      (إعدادات البناء)
```

---

## الميزات

### Windows Installer (NSIS)
- تنصيب سهل مع خيارات متقدمة
- إمكانية اختيار مسار التنصيب
- اختصارات على سطح المكتب وقائمة Start
- إلغاء تنصيب سهل

### التطبيق
- واجهة كاملة بدون متصفح
- قائمة عربية مخصصة
- اختصارات لوحة المفاتيح:
  - `Ctrl+R` - إعادة تحميل
  - `F11` - تكبير الشاشة
  - `Ctrl+Plus/Minus` - تكبير/تصغير
  - `Ctrl+0` - الحجم الطبيعي
  - `Alt+F4` - إغلاق

---

## ملاحظات مهمة

### الاتصال بـ Supabase
التطبيق يحتاج اتصال بالإنترنت للوصول إلى قاعدة البيانات Supabase.

### ملف `.env`
تأكد من وجود ملف `.env` في نفس مجلد التطبيق يحتوي على:
```env
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_key
```

---

## توزيع التطبيق

### الطريقة 1: ملف التنصيب (موصى به)
وزع ملف `Bloov-Accounting-Setup-1.0.0.exe` للمستخدمين.

### الطريقة 2: النسخة المحمولة
انسخ مجلد `win-unpacked` بالكامل - يعمل بدون تنصيب.

---

## حل المشاكل

### المشكلة: التطبيق لا يتصل بقاعدة البيانات
**الحل:** تأكد من:
1. الاتصال بالإنترنت
2. صحة بيانات Supabase في ملف `.env`
3. إعدادات الـ Firewall لا تحجب التطبيق

### المشكلة: الشاشة بيضاء عند فتح التطبيق
**الحل:**
1. أعد تشغيل التطبيق
2. تأكد من بناء الملفات بشكل صحيح: `npm run build`
3. امسح الـ cache: احذف مجلد `dist` وأعد البناء

---

## التحديثات المستقبلية

لتحديث التطبيق:
1. غير رقم الإصدار في `package.json`
2. أعد البناء: `npm run electron:build:win`
3. وزع ملف التنصيب الجديد

---

## دعم فني

في حال واجهت مشاكل:
1. تحقق من صحة إعدادات Supabase
2. تأكد من تحديث Node.js
3. امسح `node_modules` وأعد التثبيت: `npm install`

---

## الترخيص

هذا التطبيق خاص بشركة Bloov ويخضع لشروط الاستخدام الخاصة بها.
