# نظام مشاركة الفواتير عبر واتساب

## نظرة عامة
نظام قوي وموثوق لمشاركة الفواتير عبر واتساب مع آلية fallback متقدمة.

## المميزات الرئيسية

### 1. رسالة نظيفة واحترافية
```
مرحباً
شكراً لتسوقك في BLOOV
رقم الفاتورة: BLV-XXXXXX
المجموع: XX.XX ر.س
نتطلع لخدمتك مجدداً
```
- بدون إيموجي
- بدون أرقام هواتف
- نص بسيط وواضح

### 2. أولوية توليد الملفات (Priority Fallback System)

#### المستوى 1: PDF (الأولوية الأولى)
- محاولة توليد ملف PDF احترافي
- يحتوي على جميع تفاصيل الفاتورة
- رمز QR للتحقق من الفاتورة (ZATCA)
- تنسيق احترافي مطابق للمعايير

#### المستوى 2: صورة عالية الجودة (Fallback)
- إذا فشل PDF، يتم توليد صورة PNG
- دقة عالية للطباعة
- نفس التنسيق والمحتوى
- حجم مناسب للمشاركة

#### المستوى 3: رسالة نصية فقط (Last Resort)
- إذا فشلت الطرق السابقة
- رسالة نصية بدون مرفق
- يتم فتح واتساب مباشرة

### 3. مشاركة مباشرة للملف
- استخدام Native Share API
- إرفاق الملف (PDF أو صورة) مباشرة
- **لا توجد روابط** - الملف يُشارك مباشرة
- يمكن المستخدم من اختيار التطبيق (واتساب، تيليجرام، إلخ)

### 4. ضمان الجودة
- التحقق من حجم الملف قبل المشاركة
- التحقق من صلاحية Blob
- معالجة الأخطاء بشكل احترافي
- رسائل خطأ واضحة بالعربية

## آلية العمل

### خطوات التنفيذ

```typescript
1. إنشاء رسالة نصية نظيفة
2. تنظيف رقم الهاتف (إضافة +966 إذا لزم)
3. التحقق من دعم Share API
   ├─> غير مدعوم: فتح واتساب مع النص فقط
   └─> مدعوم: متابعة...

4. توليد PDF
   ├─> نجح: إنشاء File object من PDF
   └─> فشل: محاولة توليد صورة
       ├─> نجحت: إنشاء File object من الصورة
       └─> فشلت: رمي خطأ

5. التحقق من إمكانية مشاركة الملف
   ├─> يمكن: فتح Share Sheet مع الملف والنص
   └─> لا يمكن: فتح واتساب مع النص فقط

6. مشاركة ناجحة
```

### معالجة الأخطاء

#### خطأ توليد PDF
```
⚠️ يتم تلقائياً التحول لتوليد صورة
📝 يُسجل في Console للمراجعة
```

#### خطأ توليد الصورة أيضاً
```
❌ رسالة خطأ: "فشل توليد الفاتورة. حاول مرة أخرى."
```

#### إلغاء المستخدم للمشاركة
```
ℹ️ لا يتم عرض أي خطأ (AbortError)
📝 يُسجل في Console: "User cancelled share"
```

## الأكواد المهمة

### تنظيف رقم الهاتف
```typescript
let cleanPhone = customerPhone.replace(/[^0-9]/g, '');
if (cleanPhone.startsWith('00')) {
  cleanPhone = cleanPhone.slice(2);
} else if (cleanPhone.startsWith('0')) {
  cleanPhone = '966' + cleanPhone.slice(1);
} else if (!cleanPhone.startsWith('966')) {
  cleanPhone = '966' + cleanPhone;
}
```

### إنشاء ملف قابل للمشاركة
```typescript
// PDF
const file = new File([pdfBlob], fileName, { type: 'application/pdf' });

// أو صورة
const file = new File([imageBlob], fileName, { type: 'image/png' });
```

### مشاركة الملف
```typescript
const shareData: ShareData = {
  files: [fileToShare],
  text: cleanMessageText
};

if (navigator.canShare(shareData)) {
  await navigator.share(shareData);
}
```

## اختبار النظام

### السيناريوهات المختبرة
1. ✅ مشاركة ناجحة مع PDF
2. ✅ فشل PDF → نجاح الصورة
3. ✅ فشل كلاهما → رسالة نصية
4. ✅ إلغاء المستخدم → لا خطأ
5. ✅ Share API غير مدعوم → فتح واتساب مباشرة

### متطلبات الاختبار
- هاتف ذكي (Android/iOS)
- متصفح يدعم Share API
- واتساب مثبت
- اتصال بالإنترنت (للفواتير فقط)

## الملاحظات الفنية

### التوافقية
- ✅ Android: دعم كامل للـ Share API
- ✅ iOS: دعم كامل للـ Share API
- ⚠️ Desktop: Share API محدود (fallback للنص)

### الأداء
- PDF: ~1-2 ثانية
- صورة: ~1-2 ثانية
- حجم PDF: ~50-150 KB
- حجم الصورة: ~100-300 KB

### الأمان
- ❌ لا يتم رفع الملفات للسيرفر
- ❌ لا يتم تخزين الملفات
- ✅ الملف موجود في الذاكرة فقط
- ✅ يُحذف بعد المشاركة

## استكشاف الأخطاء

### "فشل توليد الفاتورة"
- تحقق من اتصال الإنترنت
- تأكد من صلاحية بيانات الفاتورة
- راجع Console للتفاصيل

### لا يفتح واتساب
- تأكد من تثبيت واتساب
- تحقق من رقم الهاتف (يجب أن يبدأ بـ 966)
- جرب فتح واتساب يدوياً أولاً

### الملف لا يظهر في واتساب
- تأكد من دعم المتصفح لـ Share API
- جرب متصفح آخر (Chrome مفضل)
- تحقق من أذونات المتصفح

## التحديثات المستقبلية

### محتمل
- [ ] دعم أنواع ملفات إضافية (Excel)
- [ ] خيار إرسال عبر البريد الإلكتروني
- [ ] حفظ الفواتير في معرض الصور
- [ ] مشاركة عدة فواتير مرة واحدة

### قيد الدراسة
- [ ] إضافة شعار الشركة للفاتورة
- [ ] تخصيص ألوان الفاتورة
- [ ] طباعة مباشرة للفاتورة
