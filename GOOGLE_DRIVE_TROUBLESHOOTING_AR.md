# دليل استكشاف أخطاء Google Drive

## المشاكل الشائعة وحلولها

### 1. خطأ: "Failed to fetch" أو "فشل في الحصول على رابط المصادقة"

**السبب المحتمل:**
- معلومات OAuth غير صحيحة
- مشكلة في الاتصال بـ Supabase
- صلاحيات المستخدم غير كافية

**الحل:**
1. تأكد من أنك مسجل دخول كـ Admin أو Super Admin
2. افتح Console في المتصفح (اضغط F12)
3. اذهب إلى تبويب Console
4. حاول الربط مرة أخرى وشاهد رسائل الخطأ
5. تأكد من أن Client ID و Client Secret صحيحة
6. جرب حذف المعلومات وإدخالها مرة أخرى

### 2. خطأ: "redirect_uri_mismatch"

**السبب:**
الـ Redirect URI في Google Cloud Console لا يتطابق مع الرابط الصحيح

**الحل:**
1. اذهب إلى [Google Cloud Console](https://console.cloud.google.com)
2. اذهب إلى "APIs & Services" > "Credentials"
3. اضغط على OAuth 2.0 Client ID الخاص بك
4. في قسم "Authorized redirect URIs"، تأكد من وجود الرابط التالي بالضبط:
   ```
   YOUR_SUPABASE_URL/functions/v1/google-drive-auth?action=callback
   ```
5. استبدل `YOUR_SUPABASE_URL` برابط السوبابيس الخاص بك
6. احفظ التعديلات وحاول الربط مرة أخرى

### 3. خطأ: "Access blocked: This app's request is invalid"

**السبب:**
OAuth consent screen غير مكتمل الإعداد أو المستخدم غير مضاف إلى Test users

**الحل:**
1. اذهب إلى [Google Cloud Console](https://console.cloud.google.com)
2. اذهب إلى "APIs & Services" > "OAuth consent screen"
3. تأكد من:
   - اسم التطبيق موجود
   - بريدك الإلكتروني موجود في User support email
   - بريدك الإلكتروني موجود في Developer contact information
4. اذهب إلى قسم "Test users"
5. اضغط "+ ADD USERS"
6. أدخل بريدك الإلكتروني الذي تريد استخدامه للربط
7. احفظ وحاول الربط مرة أخرى

### 4. خطأ: "Google Drive credentials not configured"

**السبب:**
Client ID و Client Secret غير محفوظة في قاعدة البيانات

**الحل:**
1. في صفحة النسخ الاحتياطي، اضغط "إعدادات Google Drive"
2. أدخل Client ID (يجب أن ينتهي بـ `.apps.googleusercontent.com`)
3. أدخل Client Secret (يجب أن يبدأ بـ `GOCSPX-`)
4. اضغط "حفظ معلومات OAuth"
5. انتظر رسالة التأكيد
6. الآن يمكنك الضغط على "ربط الحساب"

### 5. خطأ: "Only admins can connect Google Drive"

**السبب:**
المستخدم الحالي ليس لديه صلاحية Admin

**الحل:**
1. تأكد من أنك مسجل دخول بحساب Admin أو Super Admin
2. إذا لم تكن Admin، اطلب من المسؤول أن يقوم بالربط
3. للتحقق من صلاحياتك، اذهب إلى صفحة إدارة المستخدمين

### 6. خطأ: "Failed to save credentials"

**السبب:**
مشكلة في حفظ رموز OAuth في قاعدة البيانات

**الحل:**
1. تحقق من اتصالك بالإنترنت
2. حاول تسجيل الخروج ثم الدخول مرة أخرى
3. امسح cache المتصفح
4. حاول الربط مرة أخرى

### 7. خطأ: "Access token missing" عند الرفع

**السبب:**
رمز الوصول (Access Token) انتهت صلاحيته

**الحل:**
1. اضغط على زر "إعادة الربط"
2. سجل دخول مرة أخرى بحساب Google
3. اسمح للتطبيق بالوصول
4. جرب الرفع مرة أخرى

## كيفية فحص الأخطاء في Console

1. افتح المتصفح
2. اضغط F12 (أو كلك يمين > Inspect)
3. اذهب إلى تبويب "Console"
4. حاول القيام بالعملية التي تسبب الخطأ
5. شاهد رسائل الخطأ التي تظهر باللون الأحمر
6. انسخ رسالة الخطأ الكاملة إذا احتجت مساعدة

## التحقق من Client ID و Client Secret

### Client ID الصحيح:
- يجب أن يكون على هذا الشكل: `123456789-abcdefg.apps.googleusercontent.com`
- يحتوي على أرقام ثم شرطة ثم حروف وأرقام
- ينتهي بـ `.apps.googleusercontent.com`

### Client Secret الصحيح:
- يجب أن يبدأ بـ `GOCSPX-`
- متبوع بمجموعة من الحروف والأرقام
- مثال: `GOCSPX-abc123XYZ789`

## اختبار الربط خطوة بخطوة

1. **حفظ معلومات OAuth:**
   - أدخل Client ID
   - أدخل Client Secret
   - اضغط "حفظ معلومات OAuth"
   - انتظر رسالة "تم حفظ معلومات OAuth بنجاح"

2. **ربط الحساب:**
   - اضغط "ربط الحساب"
   - ستفتح نافذة جديدة
   - اختر حساب Google الذي تريد استخدامه
   - اضغط "Allow" أو "السماح"
   - ستُغلق النافذة تلقائياً
   - يجب أن تظهر رسالة "تم الربط بنجاح"

3. **اختبار الرفع:**
   - اضغط "رفع نسخة احتياطية إلى Google Drive"
   - انتظر حتى تكتمل العملية
   - افتح Google Drive وتحقق من وجود الملف

## نصائح إضافية

1. **استخدم Chrome أو Firefox**
   - هذه المتصفحات تعمل بشكل أفضل مع OAuth

2. **تأكد من السماح بالنوافذ المنبثقة**
   - بعض المتصفحات تحجب النوافذ المنبثقة
   - إذا لم تفتح نافذة Google، تحقق من إعدادات المتصفح

3. **استخدم نفس الحساب**
   - تأكد من استخدام نفس حساب Google في كل مرة

4. **تحقق من المساحة في Google Drive**
   - تأكد من وجود مساحة كافية لحفظ النسخ الاحتياطية

## الحصول على مساعدة إضافية

إذا لم تنجح أي من الحلول أعلاه:

1. افتح Console في المتصفح (F12)
2. انسخ جميع رسائل الخطأ
3. التقط صورة للشاشة
4. أرسل المعلومات للدعم الفني مع:
   - وصف المشكلة
   - الخطوات التي قمت بها
   - رسائل الخطأ من Console
   - لقطة الشاشة
